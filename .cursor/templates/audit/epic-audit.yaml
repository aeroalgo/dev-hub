schema: epic-audit/v1
role: back
epic_id: <epic_id>
plan_id: <plan_id>
date: YYYY-MM-DD
auditor: BACK AUDIT

summary:
  total_steps: 0
  implemented: 0
  not_implemented: 0
  deviations: 0

implemented:
  - step_id: sNN
    implement_file: implement-<epic_id>/sNN-<slug>.yaml

not_implemented:
  - step_id: sNN
    plan_title: <title from plan>
    new_shard: decompose-<epic_id>/sNN-audit-<slug>.yaml

deviations:
  - step_id: sNN
    implement_file: implement-<epic_id>/sNN-<slug>.yaml
    plan_intent: <what plan said>
    actual: <what was actually done>
    impact: low | medium | high

# Brownfield replace / cutover (shared/workflow-legacy-fallback-cleanup.mdc):
# непустой массив → новый *-audit-* shard или FAIL перед QA
legacy_surfaces_remaining: []   # path/symbol ещё живы vs plan sunset A/B
fallback_remaining: []          # soft-fail / dual-path ещё живы vs sunset C
purge_step_present: true        # false только если sunset весь n/a (greenfield)

blocked_reason: ""  # заполнить если blocked, иначе пусто
