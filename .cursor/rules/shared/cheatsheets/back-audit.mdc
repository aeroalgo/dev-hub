---
description: "Cheatsheet — BACK AUDIT hot path (≤1 экран)"
alwaysApply: false
---
# Hot path (BACK AUDIT)

1. Intent Inventory **из plan** (все FR/US — не только remapped decompose) + constitution CASE A/B
2. Assess = **behavior** evidence (presence/registry/alias ≠ satisfied)
3. Append-only `sNN-audit-*` для actionable gaps + sync index
4. Brownfield: `sunset_inventory_scan` + leftovers + `purge_step_present`
5. `converged: true` только без actionable + с behavior evidence по каждому plan FR → QA; иначе IMPLEMENT → AUDIT ↻

→ full: @.cursor/rules/back_developer/workflow-audit.mdc  
→ behavior-first: @.cursor/rules/shared/workflow-behavior-first.mdc  
→ ANALYZE ≠ AUDIT · no pytest · no MODE CONVERGE
