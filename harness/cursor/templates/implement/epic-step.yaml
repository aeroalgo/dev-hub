schema: epic-implement/v1
role: back
step_id: sNN
plan_id: <plan_id>
task_id: T-xxx
title: sNN — <title> IMPLEMENT
level: L1
status: in_progress
decompose_ref: memory-bank/back/plan/decompose-<plan_id>/sNN-<slug>.yaml
date: YYYY-MM-DD

skills_used: []

done: []

files: []

# deletes: из decompose.deletes — пути/символы, удалённые в этом шаге (FINISH: полный список выполнен).
deletes: []

# wire_complete: REQUIRED когда decompose.wire_complete_required: true
# (shared/_lean/anti-minimal-done.mdc · behavior-first §9). Иначе не status: completed.
# Slug hint (enforce/cutover/purge) → WARN в validate-decompose, не silent auto-FAIL истории.
# wire_complete:
#   call_sites: []
#   enforce_path: ""
#   dual_path_rg: ""
#   instruction_rg: ""
#   deny_old_proven: true

# tests: HARD — только executable cmds (loop after запускает как shell).
# OK (Hub dev-hub self-test): - '`bin/pytest path -q` — PASS'
# OK (Managed project): - 'capability_checks: [test.targeted]'
# OK:  - '`npm exec tsc -- --noEmit`'
# FAIL: - 'npm exec tsc -- --noEmit — passed'  (prose без backticks)
# FAIL: - {command: …, result: …}
# PASS/counts → verification_results, не в tests.
tests: []

integration_check: []

checkpoints:
  - id: cp1
    criterion: from decompose checkpoints[0]
    status: pending

resume_from: cp1

# role: integ — дополнительно обязательны:
# element_ref: memory-bank/integration/plan/decompose-<plan_id>/eNN-<slug>.yaml
# gaps: { status: none }
# grep_control: [{ back: "...", front: "..." }]
# verification_results: ["..."]
# discovery: []
