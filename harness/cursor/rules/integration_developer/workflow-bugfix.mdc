---
description: "Режим INTEG BUGFIX — contract drift"
alwaysApply: false
---
### Команда: INTEG BUGFIX
**Контекст**: @.cursor/rules/integration_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/integration_developer/isolation_rules/_lean/bugfix.mdc
**Скиллы:** query-builder · diagnosing-bugs · systematic-debugging · tdd · playwright-best-practices · playwright-generate-test · frontend-testing

**Процесс**:
1. `OK INTEG BUGFIX - Ищу причину рассинхрона` → **Gates**
2. **Вход из QA:** `bugfix-queue.yaml` из `load_now` — единственный курсор; owner выбирается по item target
3. Первый item по порядку: TDD failing test → root cause → fix → targeted contract/scenario green → done/evidence; partial между сессиями допустим. Все terminal → full verification; FAIL → gate repair в ту же queue.
4. `integration/bugfix/<epic_id>/bugfix-*.md`; report — narrative, contract update if keys
5. **FINISH:** finish-block (`code_changed: yes`); Handoff → runner next session  
   Next-mode: `memory-bank/activeContext.md` + decompose index · §BUGFIX FINISH

**Верификация**: gates + scenario + FINISH + graphify
