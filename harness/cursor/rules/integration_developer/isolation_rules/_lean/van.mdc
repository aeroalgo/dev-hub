---
description: "INTEG VAN role patch — domain/full-map scope, integration level L1-L4 and GAP next semantics"
alwaysApply: false
---
# VAN — INTEG role patch

**Shared contract:** @.cursor/rules/shared/_lean/van.mdc

## Role patch (INTEG)

1. **Artifact & ownership:**
   - Session artifact: `memory-bank/integration/van/van-YYYYMMDD-<slug>.md`
   - Brownfield refresh: все architecture shards refreshed (front нет → `frontend.md` = absent) + `memory-bank/architecture/index.md`
2. **Domain & level:**
   - Домен/scope (entity, screens, endpoints) **или** full-repo map если user не сузил
   - As-built checklist: model / endpoint / front / contract?
   - L1–L4 в van-артефакте:
     - L1: flat CRUD, 1 screen
     - L2: filters + URL sync
     - L3: joins, mapping_filters
     - L4: dashboard N API
3. **Next semantics:**
   - L1 → IMPLEMENT
   - L2+ → GAP (если нет свежего) → PLAN → DECOMPOSE
