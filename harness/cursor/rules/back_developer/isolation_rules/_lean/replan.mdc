---
description: "REPLAN gates — независимый plan outcome в пределах трёх итераций"
alwaysApply: false
---
# REPLAN — gates

## Gates

1. Вход только после PASS `BACK QA`, с `plan/<epic_id>/md/prompt.md` и номером iteration.
2. Секция `## Epic` в `md/prompt.md` — единственный критерий outcome (не Covering, не plan.md).
   Старые plan/shards/audits — только факты уже сделанного. Prompt I1 не переписывать.
3. Создаваемый plan обязан иметь `iteration: 2` или `iteration: 3`, явный
   outcome verbatim и отдельные `outcome_gap|legacy_removal|hardening` задачи.
4. Любая работа вне исходного outcome уходит в `backlog_candidates`, а не в plan.
5. После I3 новый plan запрещён. Допустим только FINAL OUTCOME REVIEW и
   точечный BUGFIX, прямо восстанавливающий outcome.

## Abort

- Нет `md/prompt.md` / нет `## Epic` / файл не начинается с `## Epic` / пустой prompt или QA PASS отсутствует → reject.
- В `prompt.md` есть H1 `# Outcome prompt` / ID эпиков / шапка Role·Deps / пути чужих plan → FAIL.
- REPLAN читает Covering как SoT / читает соседние `plan/` из-за prompt → FAIL.
- Прежний plan/shard использован как доказательство достижения outcome → FAIL.
- Создана I4 или расширен scope исходного prompt → FAIL.
- Внесены code/loop/CLI/schema изменения в рамках REPLAN → FAIL.

## Verify

`rg -n 'iteration: 2|iteration: 3|FINAL OUTCOME REVIEW|backlog_candidates' .cursor/rules/back_developer/workflow-replan.mdc`
`test -f memory-bank/back/plan/<epic_id>/md/prompt.md`

