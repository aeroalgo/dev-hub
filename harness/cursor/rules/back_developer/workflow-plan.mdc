---
description: "Режим BACK PLAN — детальный план по уровню сложности"
alwaysApply: false
---
### Команда: BACK PLAN
**Контекст**: @.cursor/rules/back_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/back_developer/isolation_rules/_lean/plan.mdc
**Phase 0 orchestration**: @.cursor/rules/shared/workflow-plan-clarify-orchestration.mdc
**CLARIFY (Phase 0 mechanics)**: @.cursor/rules/back_developer/workflow-clarify.mdc · gates `isolation_rules/_lean/clarify.mdc`
**Multi-epic**: @.cursor/rules/shared/workflow-plan-multi-epic.mdc
**Outcome prompt**: @.cursor/rules/shared/workflow-plan-outcome-prompt.mdc — `plan/<epic_id>/md/prompt.md` (`## Epic` + `## Covering`)
**Policy lazy (по триггеру, не грузить целиком):** @.cursor/rules/shared/workflow-legacy-fallback-cleanup.mdc · @.cursor/rules/shared/workflow-spec-first-replace.mdc · @.cursor/rules/shared/workflow-behavior-first.mdc

---

## Hot path

1. Phase 0 Clarify (orchestration) → Completion Report / skip → `§Product probe`
2. `SUSPENSION GUARD` · level в `tasks.md` · Product + Eng review batch (L2+)
3. Write `plan/<id>/md/plan.md`: WHAT → Technology axiom → HOW → sunset A/B/C/I → Eng spine → `#qa-consumes` ≥3 TM → Review readiness (no pending Required)
3p. Write `plan/<id>/md/prompt.md` (начинается с `## Epic`; без H1 `# Outcome prompt`; без шапки Role/Deps/ID; Covering без ID; не HOW/FR; не читать соседние plan) — shared/workflow-plan-outcome-prompt.mdc
3a. **Independent Test = behavior** (не alias/get_*/yaml field) — shared/workflow-behavior-first.mdc §5
4. Каждый PLAN → апсерт `roadmap/queue.yaml` + plan + `prompt.md`; multi-epic → N rows/plans/prompts (`## Covering` один текст, оси без ID; без roadmap md / без `roadmap/prompt-*`)
4a. **Queue reconcile (same session, every PLAN):** после апсерта queue обязательно `python loop/context_loop.py --cwd "$PROJECT_ROOT" roadmap-merge --role back` (conflict/error → halt)
5. FINISH (`code_changed: no`) → next DECOMPOSE (не ROADMAP MERGE)

→ Appetite = timebox + cut_list only (не hard cap sNN)
→ outline / advisory floor: target band **5–8** sNN (L3/L4 ≤9); не раздувать draft до 10–12 «на всякий» — shared/workflow-behavior-first.mdc §3a

---

**Скиллы — core (Phase 1):**
- @.agents/skills/writing-plans/SKILL.md
- @.agents/skills/grill-me/SKILL.md — **Phase 0 only**; Spike skip → mini grill в plan §Product probe
- @.agents/skills/python-testing-patterns/SKILL.md

**Скиллы — situational (только при триггере; 1–3 за сессию):**
- brainstorming · architecture-patterns · python-design-patterns — L3–4 / размытая
- fastapi-templates — FastAPI в techContext
- sqlalchemy-postgres — SQLAlchemy / Alembic
- supabase-postgres-best-practices — Postgres
- async-python-patterns — async / WS в scope
- websocket-engineer — realtime / WS

**Batch (Phase 1 only):** шаги 2–8 за один проход; вопросы пакетом. **Phase 0** — sequential (orchestration).

**Процесс**:
0. **Phase 0 Clarify (orchestration):** прочитай **Phase 0 orchestration** + при триггерах выполни clarify mechanics (gates + shared-core). Sequential Q до Completion Report или halt. Skip → краткая запись в plan `§Product probe`. **Не** писать полный plan до Phase 0 gate OK.
1. `OK BACK PLAN - Phase 1 Plan writing` + `SUSPENSION GUARD active — plan output unlimited` (→ token-economy-core.mdc §0.0)
1a. Прочитай **Gates** (+ Hot path если ещё не)
1b. **Clarify link:** если clarify-артефакт есть — ссылка в шапке plan + `## Clarifications` (summary, не весь Q→A)
1c. Если вход = research / audit / multi-P / ≥2 subsystem → прочитай **Multi-epic**; объяви `MULTI-EPIC PLAN — N эпиков`; **запрещён** один mega-plan
1d. **CRITICAL marker gate:** `[НУЖНО УТОЧНИТЬ: CRITICAL …]` должен иметь resolve или явный defer-list с owner/next до FINISH PLAN.
2. `tasks.md` → level (+ свободные `T-xxx` при multi-epic)
2p. **Plan review batch (после level fix):**
    - **Product pass:** product alignment / premises (Phase 0 или `§Product probe`).
    - **Eng pass:** Eng review spine — situational `architecture-patterns` при L2+.
    - **Auto-resolve defer:** в `## Plan review batch log` (не AskUserQuestion one-by-one). L1 — review опционален.
3. writing-plans; skills list в каждом `plan/<task_id>/md/plan.md`; L3–4 → situational brainstorming / architecture-patterns
3a. **Roadmap path (always):** каждый PLAN сначала апсертит канонический `roadmap/queue.yaml` (`batches.<slug>` и одну строку `queue[]` на эпик), затем сохраняет полный `plan/<epic_id>/` + `md/prompt.md`. Для multi-epic сначала выполняется epic cut и добавляется строка на **каждый** эпик (`## Epic` разный, `## Covering` один, Axes **без** ID эпиков). Для single-epic split не нужен, но queue row и batch обязательны. Шаблон: @.cursor/templates/roadmap-queue.yaml
4. Содержание по level delta в gates (L2 / L3–4). Черновик нарезки без checkbox — трекер после DECOMPOSE только в `decompose/index.md`. Brownfield → sunset **A+B+C+I** + AC− fail-closed. Technology axiom + Independent Test = behavior (не alias/get_*/yaml field).
5. CREATIVE need? (per epic при multi-epic)
6. Сохрани артефакт(ы): обязательны апсерт `memory-bank/back/roadmap/queue.yaml` + `memory-bank/back/plan/<epic_id>/md/plan.md` и `md/prompt.md` (scaffold: `python3 .claude/hooks/epic_resolve.py mb-scaffold plan --epic-id <id>`). Для multi-epic — N queue rows, N plan dirs и N `prompt.md` (Covering одинаковый).
7. `tasks.md` append-only (ссылка на план / roadmap queue + эпики; без sNN-чеклиста)
7a1. **Queue reconcile (same session, every PLAN):** после апсерта queue обязательно:
    `python loop/context_loop.py --cwd "$PROJECT_ROOT" roadmap-merge --role back`
    Conflict / unknown deps / empty active / `ok: false` → **halt PLAN**. Нельзя завершить PLAN без канонического queue.
7b. **FINISH:** @.cursor/rules/shared/finish-block.mdc (`code_changed: no`); `load_now` = clarify artifact (если был) + `roadmap/queue.yaml` + first/sole plan + `md/prompt.md` (нет prompt → FAIL)
7c. **CLARIFY FINISH guard:** PLAN **не** закрывается при открытом CRITICAL без resolve/defer-list.
7d. **Review readiness gate:** L2+ `## Review readiness` без неразрешённых `pending` Required.
8. Next: L2+ → **BACK DECOMPOSE** `queue[0]` из `roadmap/queue.yaml`; CREATIVE если дизайн; L1 → IMPLEMENT. **FORBIDDEN:** next = `BACK ROADMAP MERGE`

**Armed IMPLEMENT lock:** если `plan/<id>/yaml/decompose-index.yaml` уже в loop / BACK IMPLEMENT — этот PLAN **не** делает Write всего `md/decompose-index.md`, **не** меняет status/чеклист, **не** перезаписывает Handoff эпика. Status только `epic_resolve.py mark-index-status` / `finalize-step`.

**Верификация**: gates + AC + CREATIVE need + queue row/reconcile + multi-epic FAIL-checks + brownfield sunset (если replace) + `md/prompt.md` (начинается с `## Epic`; без H1 `# Outcome prompt`; без шапки Role/Deps/ID; без чтения соседних plan; без prompt → FAIL)
