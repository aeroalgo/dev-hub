---
description: "Backend Dev — индекс команд (BACK VAN, BACK PLAN, BACK IMPLEMENT…)"
alwaysApply: false
---
# DEV WORKFLOW — ИНДЕКС

Папка: `.cursor/rules/back_developer/`  
**Роутер:** @.cursor/rules/mainrule.mdc  
**Core:** @.cursor/rules/back_developer/mainrule-core.mdc  
**Процесс:** `workflow-*.mdc` → gates `isolation_rules/_lean/<mode>.mdc`. Скиллы — только из workflow.

## Happy path

| Режим | Процесс |
|-------|---------|
| `BACK VAN` | @.cursor/rules/back_developer/workflow-van.mdc |
| `BACK PLAN` | @.cursor/rules/back_developer/workflow-plan.mdc — Phase 0 auto-clarify + Phase 1 (+ inline roadmap-merge) |
| `BACK PLAN REFACTOR` | @.cursor/rules/back_developer/workflow-plan-refactor.mdc — purge + consolidation inventory (twins/merge/adapters) + multi-epic plan (фаза 2 cadence-блока: строго после replan pair_ids; refactor epic или noop) |
| `BACK CLARIFY` | @.cursor/rules/back_developer/workflow-clarify.mdc — добор / revisit / standalone |
| `BACK DECOMPOSE` | @.cursor/rules/back_developer/workflow-decompose.mdc |
| `BACK ANALYZE` | @.cursor/rules/back_developer/workflow-analyze.mdc |
| `BACK CREATIVE` | @.cursor/rules/back_developer/workflow-creative.mdc |
| `BACK IMPLEMENT` | @.cursor/rules/back_developer/workflow-implement.mdc |
| `BACK AUDIT` | @.cursor/rules/back_developer/workflow-audit.mdc |
| `BACK QA` | @.cursor/rules/back_developer/workflow-qa.mdc |
| `BACK ARCHIVE NOW` | @.cursor/rules/back_developer/workflow-archive.mdc |
| `BACK TASK` | @.cursor/rules/back_developer/workflow-task.mdc |
| `BACK BUGFIX` | @.cursor/rules/back_developer/workflow-bugfix.mdc |
| `BACK REFACTOR` | @.cursor/rules/back_developer/workflow-refactor.mdc |
| `BACK SECURITY` · `BACK SECURITY PLAN` · `BACK SECURITY DECOMPOSE` | @.cursor/rules/back_developer/workflow-security.mdc |

## Recovery / ad-hoc (не next после PLAN)

| Режим | Процесс |
|-------|---------|
| `BACK ROADMAP MERGE` | @.cursor/rules/back_developer/workflow-roadmap-merge.mdc — только если canon устарел без PLAN |
| `BACK RECONCILE` | @.cursor/rules/back_developer/workflow-reconcile.mdc — drift после hotfix или фаза resync cadence-блока (`--from-queue`) |
| `BACK REPLAN` | @.cursor/rules/back_developer/workflow-replan.mdc — независимый plan outcome после QA (фаза 1 cadence-блока: pair_ids, one-hop, skip evidence при отсутствии critical gaps) |
| `BACK JANITOR` | @.cursor/rules/back_developer/workflow-janitor.mdc |

## Cadence block (Kind I order)

Каденс-блок (`memory-bank/back/roadmap/cadence.yaml`) выполняется каждые N feature-эпиков (every-2) в строгом порядке: `REPLAN -> Refactor -> Resync -> Reset/Idle`:
1. `phase=replan`: REPLAN для `pair_ids` (one-hop per ID, запрет replan-of-replan; critical gaps only, non-critical → skip evidence).
2. `phase=refactor`: PLAN REFACTOR (строго после закрытия всех pair_ids в replan; refactor epic `kind: refactor` или noop evidence).
3. `phase=resync`: вызов reconcile для всех эпиков очереди `queue.yaml` (через CLI/API `--from-queue`), структурированная фиксация `ResyncEvidence` в `cadence.yaml`, инвалидация планов HOW при HIGH drift без мутации §Epic, fail-closed блокировка resume при необработанном HIGH drift.
4. `phase=idle` (reset): сброс фазы в `idle` и обнуление счётчика фич (`on_resync_done` / `reset_cadence_idle`) для возобновления движения feature-эпиков.

**Anti-carry policy:** строгий запрет (FORBIDDEN) на перенос незакрытых FR в соседние feature-эпики (no neighbor carry) и запрет скользящих окон (sliding window).

**Archive (не gate):** `workflow-reflect.mdc` — не линковать из hot path; lifecycle после QA = DONE.

Пути: @.cursor/rules/shared/memory-bank-paths.mdc (**только VAN** / структура).  
As-built: `memory-bank/architecture/` — @.cursor/rules/shared/workflow-van-brownfield.mdc.  
FINISH: @.cursor/rules/shared/finish-block.mdc  

UI → `FRONT *`. Сквозной wire → `INTEG *`.
