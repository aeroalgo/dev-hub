---
description: "Режим FRONT BUGFIX — root cause, Playwright"
alwaysApply: false
---
### Команда: FRONT BUGFIX
**Контекст**: @.cursor/rules/front_developer/mainrule-core.mdc
**Gates**: @.cursor/rules/front_developer/isolation_rules/_lean/bugfix.mdc
**Скиллы:** tdd · playwright-best-practices · playwright-generate-test · next-best-practices · vercel-react-best-practices · verification-before-completion · requesting-code-review

**Процесс**:
1. `OK FRONT BUGFIX - [суть]` → **Gates**
2. **Вход из QA:** `bugfix-queue.yaml` из `load_now` — единственный курсор; visible UI → design stack Read до UI edit
3. Первый item по порядку: reproduce → root cause → regression → fix → targeted scenario green → done/evidence; partial между сессиями допустим. Все terminal → full verification command; FAIL → gate repair в ту же queue.
4. §0.11 · pre-FINISH skills
5. **FINISH:** finish-block (`code_changed: yes`); `@verify-bugfix` проверяет queue + report → runner next session
   Next-mode: `memory-bank/activeContext.md` + decompose index · §BUGFIX FINISH

**Верификация**: gates + scenario + FINISH + graphify
