---
description: "FRONT — TDD, Playwright, visible UI, parent-only tests (только при FRONT-командах)"
alwaysApply: false
---
# FRONT — ролевой контекст

**Shared core:** @.cursor/rules/shared/role-core-contract.mdc

## ТЕСТИРОВАНИЕ

- **HARD:** Vitest/RTL/Playwright — **только parent** (never subagent): `.cursor/rules/front-tests-parent-only.mdc`.
- TDD targeted в IMPLEMENT/TASK (Hub: `bin/pytest`; Managed: `capability_checks: [test.targeted]`); полный suite / parent-only Playwright → **FRONT QA**
- **Scenario:** user-visible flow → parent-only Playwright targeted до FINISH; QA дописывает дыры. **FORBIDDEN Handoff:** «manual smoke only» без spec path
- Skill TDD: только `tdd/SKILL.md` из workflow; spawn → HARD RULE строка в промпт subagent

## DESIGN STACK (visible UI)

Канон: decompose `visible_ui` + Design skills **или** workflow-implement A + `isolation_rules/_lean/implement` gate 6. Read **до** UI-кода. `visible_ui: no` / pure lib — skip. DECOMPOSE обязан проставить `visible_ui` в каждом `sNN`.

## ПЕРЕХОДЫ

```text
VAN → PLAN(I1) → DECOMPOSE → ANALYZE → CREATIVE? → IMPLEMENT* → AUDIT → QA → REPLAN(I2/I3) ↺ → FINAL REVIEW → DONE → ARCHIVE
                 ↘ RECONCILE (hotfix drift)                         ↘ BUGFIX from AUDIT/QA
```

Next: Handoff в `memory-bank/activeContext.md`; следующий шаг — activeContext + decompose index.
Promote DECOMPOSE→IMPLEMENT: traceability CRITICAL=0 + **ANALYZE artifact critical_count=0** (workflow-decompose). **FORBIDDEN** ANALYZE deferred skip.
Post-QA: I1/I2 → explicit `FRONT REPLAN` (spawn new epic_id или skip marker; parent plan/prompt/done не overwrite). I3 → FINAL OUTCOME REVIEW. Runner/loop пока не ведёт счётчик iterations. REFLECT не gate.

Не дублировать таблицы «после QA → …» здесь — только FINISH sync ledger.
