# Loop cursor

Канон переходов: `memory-bank/activeContext.md` (`load_now` + `- **Следующий:**`) и decompose index.  
Runner: `./bin/loop` · `python3 -m loop.runner` (compatibility shim: `./loop/loop.sh`).
