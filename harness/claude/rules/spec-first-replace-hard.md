# SPEC-FIRST REPLACE (HARD — always)

Канон Cursor: `.cursor/rules/spec-first-replace-hard.mdc` (`alwaysApply: true`).

Parity: тот же текст. Полный PLAN/DECOMPOSE: `.cursor/rules/shared/workflow-spec-first-replace.mdc`.

**Суть:** as-built = что удалить, не как копировать. Pydantic → JSON на границе → regex **delete in-epic**, не «рядом».
