---
description: front DECOMPOSE — разложить готовый план на атомарные шаги (Cursor parity)
---
Apply `.claude/skills/role-command/SKILL.md` for **front DECOMPOSE**.

**HARD:** maximal detail — все этапы + все AC/UX требования плана (`## Requirements coverage`, `## Stages coverage`, `## Outcome map`, `## Replacement cleanup`). Brownfield replace → `deletes:` + `rg` cp. Не ужимать число шагов. См. `.cursor/rules/front_developer/workflow-decompose.mdc` §Replacement cleanup + BACK §Maximal detail.

$ARGUMENTS
