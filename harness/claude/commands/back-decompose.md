---
description: back DECOMPOSE — разложить готовый план на атомарные шаги (Cursor parity)
---
Apply `.claude/skills/role-command/SKILL.md` for **back DECOMPOSE**.

**HARD:** maximal detail — все этапы + все AC/FR/NFR плана в YAML index и shards (`plan_refs`, `plan_contract`, `deletes`, `out_of_scope`). Brownfield replace → `deletes:` + `rg` cp. Не ужимать число шагов. См. `.cursor/rules/back_developer/workflow-decompose.mdc` §Maximal detail + §Replacement cleanup + `.claude/rules/plan-artifact.md`.

$ARGUMENTS
