# Harness

`harness/` is the canonical Source of Truth (SoT) for repo hooks, agents, and instructions.

- `harness/` contains the behavior and business logic of execution rules.
- `.claude/` acts as a thin symlink shell referencing `harness/`.
