schema: epic-decompose/v1
role: back
step_id: s03
plan_id: T-HUB-103-event-schema-sot-unify
title: s03 — remove LoopEvent exports from schemas public facade
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Удалить экспорт `LoopEvent`, `EVENT_KINDS` и `EVENT_SCHEMA` из публичного пакета `loop/schemas/__init__.py`.
  Гарантировать fail-closed поведение: попытка импорта `from loop.schemas import LoopEvent` должна завершаться `ImportError`.

plan_contract:
  fr_ids:
    - FR-002
    - SC-001
    - SC-003
    - AC− #1
  nouns:
    - loop/schemas/__init__.py
  layout_paths:
    - memory-bank/back/plan/T-HUB-103-event-schema-sot-unify/yaml/steps/s03-remove-schema-exports-and-shim.yaml
  ac_quotes:
    - "Remove LoopEvent/EVENT_KINDS/EVENT_SCHEMA from loop/schemas/__init__.py"
    - "import from loop.schemas import LoopEvent must fail"
  plan_jumps:
    - memory-bank/back/plan/T-HUB-103-event-schema-sot-unify/md/plan.md:65-75
    - memory-bank/back/plan/T-HUB-103-event-schema-sot-unify/md/plan.md:135-155

context:
  consumes:
    - loop/schemas/__init__.py
  produces:
    - loop/schemas/__init__.py без устаревших экспортов
  plan_refs:
    - memory-bank/back/plan/T-HUB-103-event-schema-sot-unify/md/plan.md#fr-002
  files:
    - loop/schemas/__init__.py

as_built:
  - "loop/schemas/__init__.py экспортирует LoopEvent, EVENT_KINDS, EVENT_SCHEMA"

delta:
  - "Удаление импортов из loop.schemas.event в loop/schemas/__init__.py"
  - "Удаление LoopEvent, EVENT_KINDS, EVENT_SCHEMA из списка __all__"

deletes: []
wire_complete_required: false

out_of_scope:
  - "follow_up: T-HUB-104-plan-path-classifier-dedup"
  - "follow_up: T-HUB-105-roadmap-legacy-source-purge"

skills:
  code_surface: api
  impl:
    - writing-plans
    - python-testing-patterns
    - architecture-patterns

checkpoints:
  - id: cp1
    criterion: В loop/schemas/__init__.py отсутствуют упоминания LoopEvent, EVENT_KINDS, EVENT_SCHEMA
    verify: "python3 -c \"import subprocess, sys; res = subprocess.run(['rg', '-n', 'LoopEvent|EVENT_KINDS|EVENT_SCHEMA', 'loop/schemas/__init__.py'], capture_output=True, text=True); sys.exit(0 if not res.stdout.strip() else 1)\""
  - id: cp2
    criterion: Попытка импорта LoopEvent из loop.schemas падает с ImportError
    verify: "python3 -c \"import pytest; \ntry:\n    from loop.schemas import LoopEvent\n    raise AssertionError('Import should fail')\nexcept ImportError:\n    pass\""

verify: []
tdd: []
