schema: epic-decompose/v1
role: back
step_id: s01
plan_id: T-HUB-004
title: "extract_verdict: remove short-circuit, last-match wins, TDD tests"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  В `_lib.py`: удалить short-circuit `if "VERDICT: PASS" in text` в `extract_verdict`.
  Сделать так, чтобы `extract_verdict` возвращал **последнее** совпадение `VERDICT: (PASS|FAIL|BLOCKED)` по порядку в тексте (last-match wins).
  Обновить/добавить тесты в `loop/tests/test_stop_gate.py` под новую семантику.
  FR-1, FR-2, FR-3, AC+ 1, AC+ 2, NFR-3.

context:
  consumes:
    - ".claude/hooks/_lib.py — функция extract_verdict (~1169–1205), short-circuit + finditer"
    - "loop/tests/test_stop_gate.py — test_extract_verdict_uses_last_match (837–842) и соседние"
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md FR-1, FR-2, FR-3, AC+ 1, AC+ 2, NFR-3"
    - "memory-bank/audit/workflow-loop-20260816/hooks-legacy.md — short-circuit PASS и false-positive"
  produces:
    - ".claude/hooks/_lib.py — только last regex match, без short-circuit"
    - "loop/tests/test_stop_gate.py — parametrized + contract-blob тесты на last-wins"
  plan_refs:
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md FR-1, FR-2, FR-3"
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md AC+ 1, AC+ 2"
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md NFR-3 (TDD)"
  files:
    - ".claude/hooks/_lib.py"
    - "loop/tests/test_stop_gate.py"

as_built:
  - "_lib.py:1198 — if 'VERDICT: PASS' in text: return 'PASS'  # short-circuit"
  - "_lib.py:1199–1205 — затем re.finditer и возврат последнего (но short-circuit уже сработал)"
  - "test_stop_gate.py:837–842 — test_extract_verdict_uses_last_match: ожидает PASS на 'FAIL then PASS' (из-за short-circuit)"
  - "audit: hooks-legacy.md §extract_verdict — упоминание 'last wins' и баг с PASS-first"

delta:
  - "_lib.py:1198 — удалить `if 'VERDICT: PASS' in text: return 'PASS'` (и аналог для FAIL если есть)"
  - "_lib.py — оставить только цикл re.finditer + возврат последнего совпадения"
  - "_lib.py — при отсутствии совпадений вернуть None (как сейчас)"
  - "test_stop_gate.py — расширить/исправить test_extract_verdict_uses_last_match"
  - "test_stop_gate.py — добавить parametrized: ('...PASS...FAIL', 'FAIL'), ('...FAIL...PASS', 'PASS'), ('...BLOCKED...PASS', 'PASS')"
  - "test_stop_gate.py — добавить тест contract: текст с 'VERDICT: PASS' внутри CONTRACT/инструкции + финал 'VERDICT: FAIL' → 'FAIL'"
  - "Регрессия: существующие тесты stop_gate не должны сломаться"

deletes:
  - "Short-circuit блок `if 'VERDICT: PASS' in text` в _lib.py (A: logic, неверная семантика)"

out_of_scope:
  - "NEED_HUMAN messaging — это s02"
  - "Registry discovery — это s03"
  - "Alias explore — это s04"
  - "Epic re-exports delete — это s05"
  - "Posttool mirror/lock — это s06"
  - "Suite smoke — это s07"
  - "epic/core.py — DoNotTouch (AC− 1)"
  - "loop.sh / context_loop.py — T-HUB-003, не трогать"

skills:
  code_surface: python
  impl:
    - tdd
    - python-testing-patterns
    - python-anti-patterns

checkpoints:
  - id: cp1
    criterion: "extract_verdict удаляет short-circuit (нет `if \"VERDICT: PASS\" in text`)"
    verify: 'grep -n ''''VERDICT: PASS'''' in text /home/aero/PyProject/dev-hub/.claude/hooks/_lib.py || echo ''''short-circuit removed'''''
  - id: cp2
    criterion: "extract_verdict использует только last regex match"
    verify: 'grep -n ''''finditer.*VERDICT'''' /home/aero/PyProject/dev-hub/.claude/hooks/_lib.py'
  - id: cp3
    criterion: "Parametrized тест last-wins добавлен: PASS→FAIL, FAIL→PASS, BLOCKED"
    verify: 'grep -c ''''@pytest.mark.parametrize.*extract_verdict\\\|def test_extract_verdict_last'''' /home/aero/PyProject/dev-hub/loop/tests/test_stop_gate.py'
  - id: cp4
    criterion: "Contract-blob тест: instructional PASS + final FAIL → FAIL"
    verify: 'grep -c ''''VERDICT: PASS.*инструкц\\\|contract.*blob.*FAIL'''' /home/aero/PyProject/dev-hub/loop/tests/test_stop_gate.py || grep -c ''''final FAIL'''' /home/aero/PyProject/dev-hub/loop/tests/test_stop_gate.py'
  - id: cp5
    criterion: "Тесты extract_verdict проходят (TDD red→green)"
    verify: 'cd /home/aero/PyProject/dev-hub && timeout 300s .venv/bin/pytest loop/tests/test_stop_gate.py -k ''extract_verdict'' -q --tb=short 2>&1 | tail -20'

verify: []

tdd:
  - "Red: текущий тест `test_extract_verdict_uses_last_match` ожидает PASS на 'VERDICT: FAIL then VERDICT: PASS' — из-за short-circuit"
  - "Red: добавить parametrized: ('...PASS...FAIL', 'FAIL'), ('...FAIL...PASS', 'PASS'), ('...BLOCKED...PASS', 'PASS')"
  - "Red: добавить тест contract: текст с 'VERDICT: PASS' в CONTRACT + финал 'VERDICT: FAIL' → extract_verdict='FAIL'"
  - "Green: удалить short-circuit → тесты зелёные"
  - "Green: все существующие тесты stop_gate не сломаны (регрессия)"
