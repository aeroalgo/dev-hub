schema: epic-decompose/v1
role: back
step_id: s05
plan_id: T-HUB-004
title: "Delete dead epic re-exports: rm 6 stubs, rm unused import, verify rg=0"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Удалить 6 мёртвых re-export модулей: `.claude/hooks/epic/{checkpoint,context,events,index,io,state}.py`.
  Удалить неиспользуемый `import epic as _epic` из `epic_lib.py`.
  AC+ 4: rg -n 'from epic\.(checkpoint|context|events|index|io|state)' → 0; файлы отсутствуют.
  AC− 1: не удалять `epic/core.py` / `epic_lib` facade.
  Smoke: import package `epic` после delete — работает.

context:
  consumes:
    - ".claude/hooks/epic/{checkpoint,context,events,index,io,state}.py — stubs 153–556 LOC, только from .core re-export"
    - ".claude/hooks/epic/__init__.py — curated facade, импортирует напрямую из .core"
    - ".claude/hooks/epic_lib.py — import epic as _epic (unused, audit)"
    - ".claude/hooks/epic/core.py — ~3143 LOC, реальный код (не трогать)"
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md FR-8, AC+ 4, AC− 1"
    - "memory-bank/audit/workflow-loop-20260816/hooks-legacy.md — epic stubs remove, 0 imports"
  produces:
    - "Удалённые файлы: epic/checkpoint.py, context.py, events.py, index.py, io.py, state.py"
    - "epic_lib.py — без `import epic as _epic`"
    - "loop/tests/test_finish_integrity.py — smoke import `from epic import ...` (уже через facade)"
  plan_refs:
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md FR-8, AC+ 4, AC− 1"
  files:
    - ".claude/hooks/epic/checkpoint.py"
    - ".claude/hooks/epic/context.py"
    - ".claude/hooks/epic/events.py"
    - ".claude/hooks/epic/index.py"
    - ".claude/hooks/epic/io.py"
    - ".claude/hooks/epic/state.py"
    - ".claude/hooks/epic_lib.py"
    - ".claude/hooks/epic/__init__.py"
    - ".claude/hooks/epic/core.py (DoNotTouch)"

as_built:
  - "epic/checkpoint.py:1–5 — from .core import ...; __all__ = [...] (re-export only)"
  - "epic/context.py:1–5 — from .core import ...; __all__ = [...] (re-export only)"
  - "epic/events.py:1–5 — from .core import ...; __all__ = [...] (re-export only)"
  - "epic/index.py:1–6 — from .core import ...; __all__ = [...] (re-export only)"
  - "epic/io.py:1–5 — from .core import ...; __all__ = [...] (re-export only)"
  - "epic/state.py:1–5 — from .core import ...; __all__ = [...] (re-export only)"
  - "epic/__init__.py:1–42 — from .core import ... (curated, не re-export stubs)"
  - "epic_lib.py:8 — import epic as _epic (unused, AST)"
  - "rg from epic.X по репо → 0 (на момент аудита)"

delta:
  - "rm .claude/hooks/epic/checkpoint.py context.py events.py index.py io.py state.py"
  - "epic_lib.py:8 — удалить `import epic as _epic`"
  - "Проверить epic/__init__.py — не ломает facade (он импортирует напрямую из .core)"
  - "Проверить epic_lib.py __all__ — не содержит _epic (только реэкспорт имён)"
  - "Smoke: python -c 'from epic import finalize_step, mirror_verify_verdict; print(\"OK\")'"
  - "Smoke: python -c 'import epic; print(epic.__file__)' — указывает на __init__.py"

deletes:
  - ".claude/hooks/epic/checkpoint.py (A: code, re-export stub)"
  - ".claude/hooks/epic/context.py (A: code, re-export stub)"
  - ".claude/hooks/epic/events.py (A: code, re-export stub)"
  - ".claude/hooks/epic/index.py (A: code, re-export stub)"
  - ".claude/hooks/epic/io.py (A: code, re-export stub)"
  - ".claude/hooks/epic/state.py (A: code, re-export stub)"
  - "import epic as _epic в epic_lib.py:8 (A: code, unused import)"

out_of_scope:
  - "extract_verdict — это s01"
  - "NEED_HUMAN messaging — это s02"
  - "Registry discovery — это s03"
  - "Alias explore — это s04"
  - "Posttool mirror/lock — это s06"
  - "Suite smoke — это s07"
  - "epic/core.py — DoNotTouch AC− 1"
  - "epic_lib.py facade — DoNotTouch AC− 1 (только удалить unused import)"
  - "epic/__init__.py — не удалять, это curated facade"

skills:
  code_surface: python
  impl: []

checkpoints:
  - id: cp1
    criterion: "6 epic stub файлов удалены (checkpoint, context, events, index, io, state)"
    verify: "ls -la /home/aero/PyProject/dev-hub/.claude/hooks/epic/ | grep -E 'checkpoint|context|events|index|io|state' || echo 'stubs deleted'"
  - id: cp2
    criterion: "rg -n 'from epic\\.(checkpoint|context|events|index|io|state)' → 0"
    verify: |-
      rg -c "from epic\\.(checkpoint|context|events|index|io|state)" /home/aero/PyProject/dev-hub 2>/dev/null || echo "count=0 (good)"
  - id: cp3
    criterion: "epic_lib.py не содержит `import epic as _epic`"
    verify: "grep -n 'import epic as _epic' /home/aero/PyProject/dev-hub/.claude/hooks/epic_lib.py || echo 'unused import removed'"
  - id: cp4
    criterion: "AC+ 4: файлы отсутствуют + rg=0"
    verify: |-
      for f in checkpoint context events index io state; do test -f /home/aero/PyProject/dev-hub/.claude/hooks/epic/$f.py && echo EXISTS || echo DELETED; done; rg -c 'from epic\.' /home/aero/PyProject/dev-hub/.claude/hooks/ | head -5
  - id: cp5
    criterion: "Smoke: import epic package работает после delete"
    verify: "cd /home/aero/PyProject/dev-hub && python3 -c 'from epic import finalize_step, mirror_verify_verdict, load_index_yaml; print(\"epic import OK\")'"
  - id: cp6
    criterion: "Smoke: from epic import ... в тестах finish_integrity не сломан"
    verify: "cd /home/aero/PyProject/dev-hub && timeout 300s .venv/bin/pytest loop/tests/test_finish_integrity.py -q --tb=short 2>&1 | tail -10"

verify: []

tdd:
  - "Pre-delete: rg from epic.X → 0 (подтвердить, что stubs уже не используются)"
  - "Pre-delete: удалить import _epic → python -c 'import epic_lib' работает"
  - "Post-delete: 6 файлов отсутствуют"
  - "Post-delete: rg from epic.X → 0"
  - "Post-delete: import epic facade работает"
  - "Post-delete: тесты finish_integrity проходят (smoke)"
