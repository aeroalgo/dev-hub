schema: epic-decompose/v1
role: back
step_id: s06
plan_id: T-HUB-004
title: "Posttool mirror + save_state lock: remove bare except, add lock/atomic"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  В `agent-posttool.py`: убрать bare `except: pass` на mirror_verify_verdict; ошибка должна быть видна (log + не считать epic verdict обновлённым).
  В `_lib.py`: добавить защиту от lost-update на `save_state` / load_state для spawn-state path (fcntl/portalocker-style или atomic replace + lockfile).
  FR-9, FR-10: mirror ошибки видны; spawn-state не теряет update при concurrent posttool ∥ subagent-stop.

context:
  consumes:
    - ".claude/hooks/agent-posttool.py — ~110–117: try: from epic_lib import mirror...; mirror...; except: pass"
    - ".claude/hooks/_lib.py — save_state ~859–862 (plain write); load_state ~819–856; state_path ~810–816"
    - ".claude/hooks/subagent-stop.py — тоже вызывает save_state (concurrent с posttool)"
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md FR-9, FR-10, NFR-2"
    - "memory-bank/audit/workflow-loop-20260816/hooks-legacy.md — posttool swallow, save_state race"
  produces:
    - "agent-posttool.py — mirror без bare except; ошибка → stderr + skip epic mirror (не PASS)"
    - "_lib.py — save_state с lock (atomic write + lockfile) или fcntl"
    - "loop/tests/ — тест на concurrent save (опционально, smoke)"
  plan_refs:
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md FR-9, FR-10"
    - "plan/T-HUB-004-hooks-hygiene/md/plan.md NFR-2 (не ослаблять stop-gate)"
  files:
    - ".claude/hooks/agent-posttool.py"
    - ".claude/hooks/_lib.py"
    - ".claude/hooks/subagent-stop.py (consumer, не менять)"

as_built:
  - "agent-posttool.py:110–117 — if agent_type=='verify' and matched: try: from epic_lib import mirror...; mirror...; except: pass; save_state..."
  - "_lib.py:859–862 — def save_state(...): p.write_text(json.dumps(state...)) — no lock, no atomic"
  - "_lib.py:810–816 — state_path: runtime/spawn-gate/<session>.json"
  - "subagent-stop.py:110–112 — save_state после record_verdict (concurrent с posttool)"
  - "posttool:110 — pass на exception → spawn-state обновлён, epic verdict НЕ обновлён → рассинхрон"

delta:
  - "agent-posttool.py:110–117 — заменить bare except: pass на:"
  - "  except (ImportError, OSError, TypeError, ValueError) as exc:"
  - "    print(f'posttool: mirror_verify_verdict failed: {exc}', file=sys.stderr)"
  - "    # Не считаем epic verdict обновлённым; spawn-state уже записан save_state ниже"
  - "    # (или: не вызывать save_state если mirror failed? — решение: mirror fail не блокирует save_state, но epic не обновлён)"
  - "_lib.py:859–862 — save_state: использовать atomic write (write .tmp + rename) + optional lockfile"
  - "_lib.py — добавить helper atomic_write_text (если нет) или использовать существующий из epic"
  - "_lib.py — state_path: добавить .lock sibling или использовать fcntl на .json"
  - "Опционально: добавить timeout на lock (короткий, 1–2s) чтобы не deadlock"
  - "Regression: существующие тесты spawn-state / verdict_record не сломаны"

deletes:
  - "bare `except: pass` в agent-posttool.py:116"

out_of_scope:
  - "extract_verdict — это s01"
  - "NEED_HUMAN messaging — это s02"
  - "Registry discovery — это s03"
  - "Alias explore — это s04"
  - "Epic re-exports delete — это s05"
  - "Suite smoke — это s07"
  - "session_resilience.py — DoNotTouch NFR-4"
  - "epic/core.py checkpoint lock — не трогать (отдельный путь)"

skills:
  code_surface: python
  impl:
    - tdd
    - python-testing-patterns
    - python-resilience

checkpoints:
  - id: cp1
    criterion: "agent-posttool.py не содержит bare `except: pass` на mirror"
    verify: 'grep -n ''''except.*:.*pass'''' /home/aero/PyProject/dev-hub/.claude/hooks/agent-posttool.py || echo ''''bare except removed'''''
  - id: cp2
    criterion: "agent-posttool.py логирует mirror error в stderr"
    verify: 'grep -n "mirror.*failed\\\\\|sys.stderr" /home/aero/PyProject/dev-hub/.claude/hooks/agent-posttool.py'
  - id: cp3
    criterion: "save_state использует atomic write (tmp + rename) или lock"
    verify: 'grep -n "atomic\\\\\|rename\\\\\|fcntl\\\\\|lockfile\\\\\|\\\\\.tmp\\\\\." /home/aero/PyProject/dev-hub/.claude/hooks/_lib.py | head -10'
  - id: cp4
    criterion: "FR-9: mirror error видна (не silent pass)"
    verify: 'grep -A5 ''''mirror_verify_verdict'''' /home/aero/PyProject/dev-hub/.claude/hooks/agent-posttool.py'
  - id: cp5
    criterion: "FR-10: save_state защищён от lost-update"
    verify: 'grep -n ''''def save_state'''' -A 20 /home/aero/PyProject/dev-hub/.claude/hooks/_lib.py | head -30'
  - id: cp6
    criterion: "Регрессия: тесты verdict / spawn-state проходят"
    verify: 'cd /home/aero/PyProject/dev-hub && timeout 300s .venv/bin/pytest loop/tests/ -k ''verdict or spawn or state'' -q --tb=short 2>&1 | tail -15'

verify: []

tdd:
  - "Red: тест — posttool mirror exception → stderr содержит 'mirror failed', spawn-state всё равно записан"
  - "Red: тест concurrent — два save_state одновременно → один не теряется (atomic или lock)"
  - "Green: fix bare except + atomic/lock → тесты зелёные"
  - "Green: существующие test_hook_epoch_gates.py не сломаны"
