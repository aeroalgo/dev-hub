schema: epic-decompose/v1
role: back
step_id: s02
plan_id: T-HUB-099-execution-evidence-write-deny
title: "s02 — execution-evidence-write-deny-predicate — path-safe write deny reason helper in _lib.py"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Реализовать в harness/hooks/_lib.py функцию execution_evidence_write_deny_reason с O(1)
  path-safe проверкой путей memory-bank/{back,front,integration}/execution/{epic_id}/{step_id}/capability-*.json,
  возвращающую стабильный diagnostic reason и защищённую от path traversal.

plan_contract:
  fr_ids:
    - FR-001
    - FR-003
    - US-I2-001
    - US-I2-002
    - NFR-001
    - NFR-002
    - NFR-003
    - TM-I2-077-01
  nouns:
    - execution_evidence_write_deny_reason
    - recorded_artifact_write_deny_reason
    - path resolution
    - traversal protection
    - capability sidecar pattern
  layout_paths:
    - harness/hooks/_lib.py
  ac_quotes:
    - "Extend recorded_artifact_write_deny_reason (or dedicated sibling execution_evidence_write_deny_reason) to match memory-bank/{back,front,integration}/execution/{epic_id}/{step_id}/capability-*.json using path-safe glob/regex; return stable deny string."
    - "Path matching MUST use resolved relative path under project root; no directory traversal bypass."
    - "Performance: deny check is O(1) regex on path, no full events.jsonl scan for execution paths."
  plan_jumps:
    - "memory-bank/back/plan/T-HUB-099-execution-evidence-write-deny/md/plan.md:72-88"
    - "memory-bank/back/plan/T-HUB-099-execution-evidence-write-deny/md/plan.md:98-105"
    - "memory-bank/back/plan/T-HUB-099-execution-evidence-write-deny/md/plan.md:108-115"

context:
  consumes:
    - "harness/hooks/_lib.py path utilities and gate_state_write_deny_reason patterns"
    - "memory-bank/back/plan/T-HUB-099-execution-evidence-write-deny/md/plan.md §Functional requirements FR-001, FR-003"
  produces:
    - "execution_evidence_write_deny_reason helper in harness/hooks/_lib.py"
    - "export of execution_evidence_write_deny_reason for policy adapters and pretool dispatch"
  plan_refs:
    - "plan.md#functional-requirements"
    - "plan.md#nfr"
    - "plan.md#target-layout-paths"
  files:
    - harness/hooks/_lib.py

as_built:
  - "harness/hooks/_lib.py defines gate_state_write_deny_reason and recorded_artifact_write_deny_reason (scanning events.jsonl)."
  - "No dedicated O(1) predicate exists for memory-bank/*/execution/**/capability-*.json sidecar paths."

delta:
  - "Implement execution_evidence_write_deny_reason(cwd, file_path) in harness/hooks/_lib.py with relative path resolution, directory traversal check, and regex matching all role variants (back, front, integration)."
  - "Return machine-stable deny string 'execution_evidence_write_denied: capability evidence files are runtime-generated and cannot be mutated by agents'."
  - "Integrate execution_evidence_write_deny_reason call into active_context_write_deny_reason fallback chain if appropriate."

deletes: []
wire_complete_required: false

out_of_scope:
  - "wiring into WritePolicyAdapter (s03)"
  - "finish validation and non-authoritative read enforcement (s04)"
  - "sunset inventory cleanup (s05)"
  - "T-HUB-076 stack profile capability execution (follow_up: T-HUB-076)"
  - "T-HUB-078 context budget enforcement (follow_up: T-HUB-078)"
  - "T-HUB-079 orchestrator lifecycle reliability (follow_up: T-HUB-079)"
  - "T-HUB-080 workflow capability instruction parity (follow_up: T-HUB-080)"
  - "full suite → QA"

skills:
  code_surface: api
  impl: []

checkpoints:
  - id: cp1
    criterion: "execution_evidence_write_deny_reason correctly identifies back, front, and integration execution capability sidecar paths and returns stable deny string."
    verify: "python3 -c \"from harness.hooks._lib import execution_evidence_write_deny_reason; assert execution_evidence_write_deny_reason('.', 'memory-bank/back/execution/T-1/s01/capability-1234.json') is not None\""
  - id: cp2
    criterion: "Relative path resolution and traversal attempts (e.g., ../../) outside project root return None safely."
    verify: "python3 -c \"from harness.hooks._lib import execution_evidence_write_deny_reason; assert execution_evidence_write_deny_reason('.', '/tmp/other/capability-1234.json') is None\""
  - id: cp3
    criterion: "Non-execution paths (regular source files, shard YAMLs) return None without performance overhead."
    verify: "python3 -c \"from harness.hooks._lib import execution_evidence_write_deny_reason; assert execution_evidence_write_deny_reason('.', 'loop/context_loop.py') is None\""

verify:
  - "bin/pytest harness/hooks/tests/test_agent_pretool_gate_write_boundary.py -k 'test_execution_evidence_write_deny_reason' -q --tb=line"
tdd:
  - "green: implement execution_evidence_write_deny_reason in _lib.py so unit tests pass"
