## Epic

### Outcome

Active gate and verify agent instructions make the same hub-vs-managed choice as rules and skills: hub self-tests use the hub runner; managed verification uses named capability checks and typed evidence. Inventory fails if unqualified language runners reappear as a managed default.

### In

- Extend the executable instruction corpus to harness agent prompt files.
- Rewrite affected verify/gate agent texts to hub-only vs managed capability_checks branches.
- Harden inventory tests against regression, including symlink-safe discovery.

### Out

- Executor behavior, evidence schema, Write deny, incident clearing, fingerprint cache.
- Capability registry vocabulary changes.

### Done when

1. Agent prompts are part of the active corpus scan.
2. Unqualified managed pytest/cargo/npm recipes in those agents fail validation.
3. Hub and FRONT parent-only boundaries remain explicit and valid.

### Forbidden after

Small allowlist-only inventory, hand-edited agent recipes that bypass the hub/managed branch, managed fallback to hub commands in agent text.

### Chat decisions

- Spawned as REPLAN follow-up; parent Epic prompt linked from plan header Parent-prompt.

---

## Covering

n/a — single epic
