schema: epic-decompose/v1
role: back
step_id: s04
plan_id: T-HUB-091-gate-identity-sot-consolidation
title: s04 — Codex spawn-time prompt rewrite with GATE_IDENTITY + pending thread SoT bind
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Реализовать доставку GateIdentity SoT в Codex child до выполнения: при наблюдении spawn_agent в
  subagent_lifecycle.py / stream_filter переписывать/дополнять prompt блоком inject_text(SoT), связывать
  pending thread с замороженным SoT до вызова wait, и зафиксировать что post-wait SubagentStart является
  mark-only и не считается inject-доставкой.

plan_contract:
  fr_ids:
    - FR-003
    - FR-006
    - FR-010
    - SC-004
    - US-004
  nouns:
    - loop/runtime_adapters/subagent_lifecycle.py
    - harness/hooks/epic_codex_stream_filter.py
    - spawn_agent
    - prompt
    - GATE_IDENTITY
    - pending_threads
  layout_paths:
    - loop/runtime_adapters/subagent_lifecycle.py
    - harness/hooks/epic_codex_stream_filter.py
    - loop/tests/test_codex_spawn_identity_delivery.py
  ac_quotes:
    - "US-004: Как Codex parent, spawn_agent prompt получает GATE_IDENTITY до child."
    - "SC-004: Codex spawn prompt contains GATE_IDENTITY before child"
    - "FR-006: Codex на spawn_agent observe: prepend/rewrite prompt с inject_text(SoT); записать pending thread + SoT bind до wait."
    - "FR-010: Post-wait вызов SubagentStart для Codex не считается delivery inject (mark-only)."
  plan_jumps:
    - "memory-bank/back/plan/T-HUB-091-gate-identity-sot-consolidation/md/plan.md:105-185"

context:
  consumes:
    - loop/gate_identity.py
    - loop/runtime_adapters/subagent_lifecycle.py
    - harness/hooks/epic_codex_stream_filter.py
  produces:
    - loop/runtime_adapters/subagent_lifecycle.py
    - harness/hooks/epic_codex_stream_filter.py
    - loop/tests/test_codex_spawn_identity_delivery.py
  plan_refs:
    - plan.md#functional-requirements-fr-
    - plan.md#user-stories
    - plan.md#qa-consumes
  files:
    - loop/runtime_adapters/subagent_lifecycle.py
    - harness/hooks/epic_codex_stream_filter.py
    - loop/tests/test_codex_spawn_identity_delivery.py

as_built:
  - "loop/runtime_adapters/subagent_lifecycle.py обрабатывает spawn_agent, регистрирует threads и вызывает subagent-start post-wait"
  - "harness/hooks/epic_codex_stream_filter.py фильтрует поток Codex CLI"

delta:
  - "При обнаружении spawn_agent модифицировать payload/prompt, добавляя inject_text(SoT)"
  - "Сохранять привязку pending thread id к текущему GateIdentity SoT"
  - "Исключить зависимость доставки от post-wait start hook (сделать post-wait mark-only)"
  - "Добавить тесты проверки наличия GATE_IDENTITY в prompt субагента Codex до его старта (TM-004)"

deletes: []

out_of_scope:
  - "Codex stop transport_bind -> s05"
  - "Claude lifecycle -> s03"

skills:
  code_surface: api
  impl: []

checkpoints:
  - id: cp1
    criterion: "Codex spawn_agent prompt contains GATE_IDENTITY header and frozen SoT fields before child runs"
    verify: "bin/pytest loop/tests/test_codex_spawn_identity_delivery.py -k test_spawn_prompt_contains_gate_identity -q"
  - id: cp2
    criterion: "Pending thread is bound to frozen GateIdentity on spawn observe"
    verify: "bin/pytest loop/tests/test_codex_spawn_identity_delivery.py -k test_pending_thread_bound_to_sot -q"

verify:
  - "bin/pytest loop/tests/test_codex_spawn_identity_delivery.py -q"
tdd: []
