schema: epic-decompose/v1
role: back
plan_id: T-HUB-018-loop-incident-autopilot
step_id: s09
title: "Docs + optional 015 stub — WORKFLOW.md + board soft hook + runbook update"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Документация tier1-autopilot системы завершена:
  1. loop/WORKFLOW.md обновлён — раздел §Incident Autopilot (tier0 → tier1 flow, eligibility, escalation, CLI commands).
  2. Runbook-шаблоны (loop/incidents/runbooks/) дополнены tier1-специфичными recovery шагами.
  3. Soft-интеграция с T-HUB-015: если DSH_MB_BRIDGE present — board execution status 'failed'
     при escalation (feature-detect, no hard dep). Если API нестабилен — заглушка + defer note.
  Docs-only шаг: нет новых Python-модулей, кроме опционального feature-detect stub.

as_built:
  - "loop/WORKFLOW.md существует (T-HUB-016/017 расширяли)"
  - "loop/incidents/runbooks/ создана в T-HUB-017 с базовыми runbook файлами"
  - "нет раздела tier1 autopilot в WORKFLOW.md"
  - "нет board failed-status mapping для incidents"
delta:
  - "MODIFY loop/WORKFLOW.md — добавить §Incident Autopilot: (1) описание tier0→tier1 flow; (2) eligibility criteria (orchestration-only, не product tests); (3) max attempts + escalation; (4) CLI: incident-status / incident-retry; (5) env vars: EPIC_INCIDENT_TIER1, EPIC_ALERT_WEBHOOK_URL, EPIC_TIER1_MAX_ATTEMPTS"
  - "MODIFY loop/incidents/runbooks/ — добавить/обновить tier1_recovery.md: шаги ручного восстановления после escalation (проверить activeContext, сбросить через incident-retry, что делать с NEED_HUMAN файлом)"
  - "ADD loop/incidents/board_soft.py (если DSH_MB_BRIDGE feature-detect возможен) — функция try_mark_board_execution_failed(incident, project_root): (1) try import DSH_MB_BRIDGE adapter; (2) если есть — post metadata incident_id to board execution; (3) любая ошибка → log warning, продолжить (fail-soft); (4) если нет bridge → no-op. Если API нестабилен → заглушка с Deferred: comment."
  - "MODIFY loop/incidents/alert.py — вызывать try_mark_board_execution_failed после write_need_human_file (feature-detect, не обязательный)"
  - "ADD loop/tests/test_incidents_board_soft.py — unit: без DSH_MB_BRIDGE → no-op без exception; с mock bridge → вызов post metadata; bridge exception → log + continue (fail-soft)"
deletes: []
out_of_scope:
  - "полная T-HUB-015 интеграция (hard dep — не в этом эпике)"
  - "UI изменения (015 frontend)"
  - "regression suite (s10)"

context:
  consumes:
    - "plan §FR-011: Integration soft — DSH_MB_BRIDGE или 015 plugin → board execution failed (feature-detect)"
    - "plan §CREATIVE: optional только если 015 API unstable at DECOMPOSE"
    - "plan §Alert channels: Soft 015: board execution failed + metadata incident_id"
    - "plan §US-005: видеть failed incident на board (если 015 есть)"
    - "loop/incidents/alert.py (из s06)"
    - "loop/WORKFLOW.md (существующий)"
    - "loop/incidents/runbooks/ (из 017)"
  produces:
    - "loop/WORKFLOW.md (modified)"
    - "loop/incidents/runbooks/tier1_recovery.md (new/modified)"
    - "loop/incidents/board_soft.py (new, feature-detect)"
    - "loop/incidents/alert.py (modified — вызов board_soft)"
    - "loop/tests/test_incidents_board_soft.py"
  plan_refs:
    - "plan-T-HUB-018 §FR-011"
    - "plan-T-HUB-018 §US-005"
    - "plan-T-HUB-018 §CREATIVE: optional"
    - "plan-T-HUB-018 §Out of scope: No 015 hard dep"
  files:
    - "loop/WORKFLOW.md"
    - "loop/incidents/runbooks/tier1_recovery.md"
    - "loop/incidents/board_soft.py"
    - "loop/incidents/alert.py"
    - "loop/tests/test_incidents_board_soft.py"

skills:
  code_surface: docs
  impl:
    - ".agents/skills/tdd/SKILL.md"
    - ".agents/skills/python-testing-patterns/SKILL.md"
    - ".agents/skills/python-error-handling/SKILL.md"

checkpoints:
  - id: cp1
    criterion: "loop/WORKFLOW.md содержит раздел §Incident Autopilot с env vars + CLI commands"
    verify: "`rg 'Incident Autopilot' loop/WORKFLOW.md && rg 'incident-retry' loop/WORKFLOW.md`"
  - id: cp2
    criterion: "tier1_recovery.md содержит шаги с incident-retry + NEED_HUMAN"
    verify: "`rg 'incident-retry' loop/incidents/runbooks/tier1_recovery.md`"
  - id: cp3
    criterion: "board_soft: без bridge → no-op без exception (fail-soft)"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_board_soft.py -k no_bridge_no_op -q --tb=line`"
  - id: cp4
    criterion: "board_soft: bridge exception → log warning + continue (fail-soft)"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_board_soft.py -k bridge_exception_fail_soft -q --tb=line`"

verify:
  - "`rg 'Incident Autopilot' loop/WORKFLOW.md`"
  - "`rg 'incident-retry' loop/incidents/runbooks/tier1_recovery.md`"
  - "`.venv/bin/pytest loop/tests/test_incidents_board_soft.py -q --tb=line`"

tdd:
  - "test_board_soft_no_bridge_is_noop"
  - "test_board_soft_bridge_posts_metadata"
  - "test_board_soft_bridge_exception_fail_soft"
  - "test_board_soft_no_secrets_in_payload"

requirements_covered:
  - FR-011
  - "US-005 (soft board integration)"
  - "plan §CREATIVE: optional board soft path"
  - "plan §Out of scope: No 015 hard dep"
  - "AC− #6 partial (fail-soft board call)"

deferred:
  - "Полная board API интеграция T-HUB-015 если API нестабилен — skip board_soft.py, только docs + Deferred comment в alert.py"
