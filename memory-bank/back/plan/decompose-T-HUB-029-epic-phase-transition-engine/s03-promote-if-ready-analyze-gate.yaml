schema: epic-decompose/v1
role: back
step_id: s03
plan_id: T-HUB-029-epic-phase-transition-engine
decompose_ref: memory-bank/back/plan/decompose-T-HUB-029-epic-phase-transition-engine/s03-promote-if-ready-analyze-gate.yaml
title: "promote_if_ready replace decompose promote; ANALYZE gate enforce"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Реализовать `promote_if_ready(cwd, epic_id, role) -> dict | None` в `loop/epic_transition.py`.
  Заменяет `promote_decompose_phase_if_ready` в context_loop.py:
  - проверяет index.yaml complete → если ANALYZE gate required → arm_phase(ANALYZE); иначе arm_phase(IMPLEMENT)
  - добавляет шим `promote_decompose_phase_if_ready` с _legacy_warn + delegate в promote_if_ready
  AC+: после DECOMPOSE FINISH с analyze-gate=required → Handoff ANALYZE (не IMPLEMENT).

context:
  consumes:
    - loop/epic_transition.py                    # s01/s02: resolve_next, arm_phase реализован
    - loop/context_loop.py:1107                  # promote_decompose_phase_if_ready — as-built
    - loop/context_loop.py:1162                  # caller (prepare_context)
    - loop/context_loop.py:1744                  # caller (sync/promote branch)
    - loop/analyze_gate.py                       # analyze_required_before_implement, gate logic
    - loop/tests/test_context_loop.py            # regression baseline (promote path)
  produces:
    - loop/epic_transition.py                    # promote_if_ready реализован
    - loop/context_loop.py                       # promote_decompose_phase_if_ready → shim+delegate
    - loop/tests/test_epic_transition.py         # promote_if_ready tests: analyze-gate enforced
    - loop/tests/test_context_loop.py            # regression: promote path через shim
  plan_refs:
    - FR-003
    - FR-004
    - US-001
    - NFR-1
  files:
    - loop/epic_transition.py
    - loop/context_loop.py
    - loop/tests/test_epic_transition.py
    - loop/tests/test_context_loop.py

as_built:
  - "loop/context_loop.py:1107 — promote_decompose_phase_if_ready: index.yaml completed check → сразу arm_epic с mode=IMPLEMENT; НЕТ проверки analyze_required_before_implement"
  - "loop/context_loop.py:1162,1744 — два caller'а promote_decompose_phase_if_ready (prepare_context + inner branch)"
  - "loop/analyze_gate.py — analyze_required_before_implement(cwd, epic_id, role) — gate predicat; не вызывается из promote path"
  - "loop/epic_transition.py — promote_if_ready stub (NotImplementedError) после s01"

delta:
  - 'loop/epic_transition.py: `promote_if_ready` — реализован: (1) читает index.yaml; (2) если все sNN completed: (3) вызывает analyze_required_before_implement(cwd, epic_id, role) из loop.analyze_gate; (4) если True → arm_phase(cwd, epic_id, "ANALYZE", role); (5) иначе → arm_phase(cwd, epic_id, "IMPLEMENT", role); (6) возвращает result dict с reason="analyze_gate" | "implement_promote"'
  - 'loop/epic_transition.py: promote_if_ready → если index не all completed → return None'
  - 'loop/context_loop.py:promote_decompose_phase_if_ready — тело заменяется: `_legacy_warn("promote_decompose_phase_if_ready"); from loop.epic_transition import promote_if_ready as _pir; return _pir(cwd, epic_id, role)` (параметры выводятся из существующих kwargs)'
  - 'loop/tests/test_epic_transition.py: test_promote_if_ready_analyze_gate_required — все sNN done + analyze gate required → assert result["armed_step"] == "ANALYZE"'
  - 'loop/tests/test_epic_transition.py: test_promote_if_ready_no_gate_goes_implement — все sNN done + gate not required → assert result["armed_step"] contains IMPLEMENT'
  - 'loop/tests/test_epic_transition.py: test_promote_if_ready_incomplete_index_returns_none — не все sNN completed → None'
  - 'loop/tests/test_context_loop.py: regression test что shim promote_decompose_phase_if_ready делегирует и возвращает совместимый dict'

deletes:
  - 'loop/context_loop.py: тело promote_decompose_phase_if_ready (кроме shim строк) → перенесено в epic_transition.promote_if_ready'
  # Shim-функция остаётся; тело удалено; полный sunset shim — s12

out_of_scope:
  - "phase_registry.yaml loader — s06"
  - "sync_cursor ANALYZE gate skip — s04"
  - "stop-gate wiring — s08"

skills:
  code_surface: backend-core
  impl:
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/python-testing-patterns/SKILL.md
    - .agents/skills/diagnosing-bugs/SKILL.md

checkpoints:
  - id: cp1
    criterion: >
      test_promote_if_ready_analyze_gate_required PASS: promote_if_ready → ANALYZE arm когда gate required
    verify: "timeout 120s .venv/bin/pytest loop/tests/test_epic_transition.py::test_promote_if_ready_analyze_gate_required -q --tb=short"
  - id: cp2
    criterion: >
      test_promote_if_ready_no_gate_goes_implement PASS
    verify: "timeout 120s .venv/bin/pytest loop/tests/test_epic_transition.py::test_promote_if_ready_no_gate_goes_implement -q --tb=short"
  - id: cp3
    criterion: >
      test_promote_if_ready_incomplete_index_returns_none PASS
    verify: "timeout 120s .venv/bin/pytest loop/tests/test_epic_transition.py::test_promote_if_ready_incomplete_index_returns_none -q --tb=short"
  - id: cp4
    criterion: >
      Регрессия: test_context_loop.py + test_roadmap_queue.py зелёные
    verify: "timeout 300s .venv/bin/pytest loop/tests/test_context_loop.py loop/tests/test_roadmap_queue.py -q --tb=line"

verify:
  - "timeout 300s .venv/bin/pytest loop/tests/test_epic_transition.py loop/tests/test_context_loop.py loop/tests/test_roadmap_queue.py -q --tb=line"

tdd:
  - "test_promote_if_ready_analyze_gate_required — all sNN done + gate=true → ANALYZE"
  - "test_promote_if_ready_no_gate_goes_implement — all sNN done + gate=false → IMPLEMENT"
  - "test_promote_if_ready_incomplete_index_returns_none"
  - "Regression: test_context_loop.py shim path green"
