schema: epic-decompose/v1
role: back
step_id: s01
plan_id: T-HUB-016
title: "Gap matrix doc + version pin — bridge known gaps catalogued for T-HUB-008"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Исследовать совместимые версии `@deepseek-ai/dsh` и `@deepseek-ai/dsh-hooks-claude-code`;
  записать hook-by-hook gap matrix в `dsh/README.md` §Hooks bridge (≥ 5 строк, owner-epic для каждой дыры);
  зафиксировать подтверждённые pins в docs.
  Outcome: T-HUB-008 точно знает scope «только gaps», gap matrix совпадает с AC− bridge (SC-002), NFR-4 соблюдён.

context:
  consumes:
    - dsh/README.md  # существующий skeleton
    - .claude/settings.json  # hooks key — list hook events + matchers
    - plan-T-HUB-016-dsh-cc-hooks-bridge.md §«Gap matrix» (таблица событий)
  produces:
    - dsh/README.md  # добавить/дополнить §Hooks bridge gap matrix + §Version pinning
    - dsh/patches/package.json  # добавить pinned @deepseek-ai/dsh-hooks-claude-code version note
  plan_refs:
    - FR-001  # pin versions
    - FR-006  # gap matrix с owner
    - NFR-4   # version pinning
    - SC-002  # gap matrix ≥ 5 rows with owner epic
    - US-004  # explicit «что bridge не даёт»
    - AC+US-004
    - AC−-005
  files:
    - dsh/README.md
    - dsh/patches/package.json

as_built:
  - "dsh/README.md: существует, содержит §Version pinning с placeholder VERSION; §Board Arm/Loop; нет §Hooks bridge gap matrix"
  - "dsh/patches/package.json: есть, содержит координаты патча dsh-phase-models; нет dsh-hooks-claude-code"
  - ".claude/settings.json hooks: 7 событий (SessionStart, UserPromptSubmit, PreToolUse×2, PostToolUse×2, SubagentStart, SubagentStop, Stop)"

delta:
  - "dsh/README.md: добавить новую секцию §Hooks bridge (под §Version pinning или выше §Running): таблица событий bridge (hook event | python hook | bridge status | mount action | gap owner)"
  - "dsh/README.md: заполнить таблицу по данным из plan §Gap matrix (9 строк: SessionStart, UserPromptSubmit, PreToolUse Agent|Task, PreToolUse Bash, PostToolUse Agent|Task, PostToolUse Bash, SubagentStart, SubagentStop, Stop)"
  - "dsh/README.md §Version pinning: добавить строку `@deepseek-ai/dsh-hooks-claude-code@VERSION` с инструкцией pin-before-install"
  - "dsh/patches/package.json: добавить комментарий/поле с pinned bridge package — либо peer note, либо dev deps note"

deletes: []
# s01 — docs-only additive; нет файлов на удаление.
# reserved comment в cordis.patch.yml удаляется в s02 (cutover шага bridge fragment).

out_of_scope:
  - "Реальная установка dsh-hooks-claude-code npm пакета (s02)"
  - "Создание Cordis fragment (s02)"
  - "Изменение cordis.patch.yml профилей (s02)"
  - "Изменение Python hooks (s05)"

skills:
  code_surface: infra
  impl:
    # docs-only step — Core не применяется (нет Python кода); impl пустой
    []

checkpoints:
  - id: cp1
    criterion: "dsh/README.md содержит §Hooks bridge с таблицей ≥ 5 строк, у каждой строки — поле owner (T-HUB-008 или self-limit)"
    verify: "grep -c 'T-HUB-008\\|self-limit' dsh/README.md | grep -E '[5-9]|[1-9][0-9]'"
  - id: cp2
    criterion: "Таблица содержит все 9 hook-событий из settings.json (SessionStart, UserPromptSubmit, PreToolUse×2, PostToolUse×2, SubagentStart, SubagentStop, Stop)"
    verify: "grep -c 'SubagentStop\\|SubagentStart\\|UserPromptSubmit\\|SessionStart\\|PreToolUse\\|PostToolUse\\|Stop ' dsh/README.md | grep -E '[7-9]|[1-9][0-9]'"
  - id: cp3
    criterion: "dsh/README.md §Version pinning упоминает dsh-hooks-claude-code с инструкцией по pin"
    verify: "grep -q 'dsh-hooks-claude-code' dsh/README.md"

verify:
  - "grep -q 'Hooks bridge' dsh/README.md"
  - "grep -q 'dsh-hooks-claude-code' dsh/README.md"
  - "grep -q 'T-HUB-008' dsh/README.md"

tdd: []
# Нет pytest для docs-only — cp verify через grep/rg.
