schema: epic-decompose/v1
role: back
plan_id: T-HUB-019-dsh-board-sync-enrichments
step_id: s07
title: "Regression suite (FakeClient e2e) + README Board sync enrichments"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Сквозной regression suite через FakeClient проверяет все enrichments вместе:
  body в description, backlog status, move-after-upsert, footer round-trip.
  README документирует новый формат для эксплуатации.

as_built:
  - "loop/tests/test_board_sync_sync.py: нет сквозного теста для body+status+move совместно"
  - "loop/README.md: нет раздела о footer delimiter, backlog column, move-after-upsert"

delta:
  - "ADD test_full_sync_step_running: WorkItem(status=in_progress, shard_rel=...) → BoardTask(status=running); description содержит body + delimiter"
  - "ADD test_full_sync_gate_backlog_move: GateWorkItem(gate_phase=DECOMPOSE, plan_rel=...) → BoardTask(status=backlog); FakeClient.moves содержит (id, 'backlog')"
  - "ADD test_full_sync_gate_post_phase_todo: GateWorkItem(gate_phase=QA) → BoardTask(status=todo); moves не содержит id"
  - "ADD test_full_sync_description_footer_roundtrip: parse_metadata(card.description) не бросает ValueError"
  - "EDIT loop/README.md: добавить раздел '## Board sync enrichments' — footer delimiter, backlog, move-after-upsert, body_loaders"

deletes: []

out_of_scope:
  - "E2E тесты с реальным DSH host"
  - "install-mb-bridge.sh сборка (parent запускает вручную после s06)"

context:
  consumes:
    - "loop/board_sync/sync.py — apply_ops (все s01–s06 реализованы)"
    - "loop/board_sync/client.py — FakeClient (с moves после s05)"
    - "loop/board_sync/body_loaders.py — load_step_body, load_gate_body (s02–s03)"
    - "loop/board_sync/diff.py — desired_cards, compute_ops (s04)"
  produces:
    - "loop/tests/test_board_sync_sync.py (дополнен regression тестами)"
    - "loop/README.md (дополнен)"
  plan_refs:
    - "plan-T-HUB-019 NFR-006, NFR-007"
  files:
    - "loop/tests/test_board_sync_sync.py"
    - "loop/README.md"

skills:
  code_surface: service
  impl:
    - ".agents/skills/tdd/SKILL.md"
    - ".agents/skills/python-testing-patterns/SKILL.md"

checkpoints:
  - id: cp1
    criterion: "test_full_sync_step_running и test_full_sync_description_footer_roundtrip зелёные"
    verify: "`.venv/bin/pytest loop/tests/test_board_sync_sync.py -k 'full_sync_step_running or footer_roundtrip' -q --tb=line`"
  - id: cp2
    criterion: "test_full_sync_gate_backlog_move зелёный; FakeClient.moves проверен"
    verify: "`.venv/bin/pytest loop/tests/test_board_sync_sync.py -k full_sync_gate_backlog -q --tb=line`"
  - id: cp3
    criterion: "test_full_sync_gate_post_phase_todo зелёный"
    verify: "`.venv/bin/pytest loop/tests/test_board_sync_sync.py -k full_sync_gate_post -q --tb=line`"
  - id: cp4
    criterion: "README содержит раздел Board sync enrichments"
    verify: "`rg -n 'Board sync enrichments' loop/README.md`"

verify:
  - "`.venv/bin/pytest loop/tests/test_board_sync_sync.py -q --tb=line`"

tdd:
  - "test_full_sync_step_running"
  - "test_full_sync_gate_backlog_move"
  - "test_full_sync_gate_post_phase_todo"
  - "test_full_sync_description_footer_roundtrip"

requirements_covered:
  - "NFR-006 (regression suite)"
  - "NFR-007 (README docs)"
