schema: epic-decompose/v1
role: back
step_id: s01
plan_id: T-HUB-005
title: "Cheatsheet BACK IMPLEMENT hot path — create back-implement.mdc ≤40 lines"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Новый агент видит BACK IMPLEMENT hot path за 1 экран (≤40 строк): load_now → graphify →
  TDD targeted → FINISH 5 точек → verify. Прямые ссылки на workflow-implement.mdc.
  Закрывает FR-1, AC+ #1, NFR-3.

context:
  consumes:
    - .cursor/rules/back_developer/workflow-implement.mdc   # источник ссылок
    - .cursor/rules/shared/finish-block.mdc                 # FINISH 5 точек
  produces:
    - .cursor/rules/shared/cheatsheets/back-implement.mdc   # NEW FILE
  plan_refs:
    - plan-T-HUB-005-simplify-docs.md §FR-1 §AC+ #1 §NFR-3
  files:
    - .cursor/rules/shared/cheatsheets/back-implement.mdc  # CREATE

as_built:
  - .cursor/rules/shared/cheatsheets/ — ОТСУТСТВУЕТ (NOT_EXISTS до этого шага)
  - workflow-implement.mdc существует; перечисляет Gates → impl-skills → TDD → FINISH

delta:
  - Создать директорию .cursor/rules/shared/cheatsheets/ (mkdir -p)
  - |
    Создать back-implement.mdc ≤40 строк с секциями:
    ## Hot path (BACK IMPLEMENT);
    1. load_now → work shard sNN + index.yaml;
    2. graphify query codebase;
    3. Read Impl skills (из sNN) до prod-edit;
    4. TDD red → code → .venv/bin/pytest → green;
    5. FINISH: 5 точек (finish-block.mdc);
    → verify: @.cursor/rules/back_developer/workflow-implement.mdc;
    → FINISH: @.cursor/rules/shared/finish-block.mdc

deletes: []
# Greenfield — нет устаревающих файлов

out_of_scope:
  - workflow-implement.mdc самого не менять (это делает s03)
  - не копировать весь workflow — только 5-bullet map

skills:
  code_surface: infra
  impl: []
  # docs-only step — Core-четыре не применимы; нет Python-кода

checkpoints:
  - id: cp1
    criterion: "Файл создан: ls .cursor/rules/shared/cheatsheets/back-implement.mdc → exit 0"
    verify: "ls .cursor/rules/shared/cheatsheets/back-implement.mdc"
  - id: cp2
    criterion: "≤40 строк: wc -l .cursor/rules/shared/cheatsheets/back-implement.mdc → ≤40"
    verify: "wc -l .cursor/rules/shared/cheatsheets/back-implement.mdc"
  - id: cp3
    criterion: "Содержит ссылки на workflow-implement.mdc и finish-block.mdc"
    verify: "rg 'workflow-implement|finish-block' .cursor/rules/shared/cheatsheets/back-implement.mdc"

verify: []
tdd: []
