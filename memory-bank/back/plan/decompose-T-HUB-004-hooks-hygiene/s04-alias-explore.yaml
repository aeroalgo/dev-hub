schema: epic-decompose/v1
role: back
step_id: s04
plan_id: T-HUB-004
title: "ALIAS explore: add ALIAS[\"explore\"]=\"explorer\", test normalization"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Добавить `ALIAS["explore"] = "explorer"` в `.claude/hooks/_lib.py`.
  Убедиться что `normalize_type("explore")` возвращает `"explorer"`.
  AC+ 6: spawn type `explore` нормализуется к `explorer` в agent-pretool.
  explorer.md заявляет alias `explore` — code должен поддерживать.

context:
  consumes:
    - ".claude/hooks/_lib.py — ALIAS: dict[str, str] = {} ~23; normalize_type ~1062–1067"
    - ".claude/hooks/agent-pretool.py — normalize_type(raw_type) ~172"
    - ".claude/agents/explorer.md — 'Ты subagent `explorer` (alias explore)' ~18"
    - "plan-T-HUB-004-hooks-hygiene.md FR-7, AC+ 6"
    - "memory-bank/audit/workflow-loop-20260816/hooks-legacy.md — explorer alias explore не нормализуется"
  produces:
    - ".claude/hooks/_lib.py — ALIAS['explore'] = 'explorer'"
    - "loop/tests/ — тест alias normalization (explore → explorer)"
  plan_refs:
    - "plan-T-HUB-004-hooks-hygiene.md FR-7, AC+ 6"
    - "plan-T-HUB-004-hooks-hygiene.md NFR-4 (agents md overlay schema — только alias claim)"
  files:
    - ".claude/hooks/_lib.py"
    - ".claude/hooks/agent-pretool.py"
    - ".claude/agents/explorer.md"
    - "loop/tests/test_stop_gate.py (или test_agent_pretool.py)"

as_built:
  - "_lib.py:23 — ALIAS: dict[str, str] = {} (пустой)"
  - "_lib.py:1062–1067 — def normalize_type(name): if name in ALIAS: return ALIAS[name]; return name"
  - "agent-pretool.py:172 — raw_type = tool_input.get('subagent_type') or ...; norm = normalize_type(raw_type)"
  - "explorer.md:18 — 'Ты subagent `explorer` (alias explore)' — claim без поддержки в code"
  - "agent_registry.py — не использует ALIAS (отдельный механизм MODEL_ALIASES)"

delta:
  - "_lib.py:23 — добавить `ALIAS: dict[str, str] = {'explore': 'explorer'}`"
  - "Проверить что normalize_type('explore') → 'explorer' (уже через dict lookup)"
  - "Проверить что normalize_type('explorer') → 'explorer' (не в ALIAS → return as-is)"
  - "Добавить тест: normalize_type('explore') == 'explorer'"
  - "Добавить тест: spawn type 'explore' в pretool → norm='explorer' (end-to-end через pretool)"
  - "explorer.md — claim уже корректен, не менять (только code должен догнать)"

deletes:
  - "n/a — добавление, не удаление"

out_of_scope:
  - "extract_verdict — это s01"
  - "NEED_HUMAN messaging — это s02"
  - "Registry discovery — это s03"
  - "Epic re-exports delete — это s05"
  - "Posttool mirror/lock — это s06"
  - "Suite smoke — это s07"
  - "agent_registry.py MODEL_ALIASES — не трогать (отдельный механизм)"
  - "agents md overlay schema — DoNotTouch NFR-4 (кроме alias claim, который уже в md)"

skills:
  code_surface: python
  impl:
    - tdd
    - python-testing-patterns

checkpoints:
  - id: cp1
    criterion: "_lib.py содержит ALIAS['explore'] = 'explorer'"
    verify: 'grep -n ''''ALIAS.*explore.*explorer\\\\\|\\"explore\\": \\"explorer\\"'''''''' /home/aero/PyProject/dev-hub/.claude/hooks/_lib.py'''''
  - id: cp2
    criterion: "normalize_type('explore') == 'explorer'"
    verify: 'cd /home/aero/PyProject/dev-hub && python3 -c ''''from .claude.hooks._lib import normalize_type; print(normalize_type(\\"explore\\"))'''''''' 2>/dev/null || python3 -c ''''''''import sys; sys.path.insert(0, \\".claude/hooks\\"); from _lib import normalize_type; print(normalize_type(\\"explore\\"))'''''''''''''
  - id: cp3
    criterion: "normalize_type('explorer') == 'explorer' (as-is)"
    verify: 'cd /home/aero/PyProject/dev-hub && python3 -c ''''import sys; sys.path.insert(0, \".claude/hooks\"); from _lib import normalize_type; print(normalize_type(\"explorer\"))'''''''''''''
  - id: cp4
    criterion: "Тест alias normalization: explore → explorer"
    verify: 'grep -c "def test.*alias.*explore\\|normalize_type.*explore" /home/aero/PyProject/dev-hub/loop/tests/ || echo "test not found yet""'
  - id: cp5
    criterion: "AC+ 6: spawn type explore нормализуется к explorer (pretool path)"
    verify: 'cd /home/aero/PyProject/dev-hub && timeout 300s .venv/bin/pytest loop/tests/ -k ''alias or explore'' -q --tb=short 2>&1 | tail -15'

verify: []

tdd:
  - "Red: тест normalize_type('explore') ожидает 'explorer' — сейчас вернёт 'explore' (ALIAS пустой)"
  - "Red: тест pretool с subagent_type='explore' → norm='explore' (не explorer) — FAIL"
  - "Green: добавить ALIAS['explore']='explorer' → тесты зелёные"
  - "Green: существующие тесты pretool не сломаны"
