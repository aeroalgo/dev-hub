schema: epic-decompose/v1
role: back
step_id: s02
plan_id: T-HUB-059-harness-claude-agents-sot-complete
title: "git mv .claude/skills → harness/claude/skills + symlink (SoT skills/role-command)"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Перенести `.claude/skills` (включая `role-command/`) в `harness/claude/skills` как единственный SoT;
  заменить `.claude/skills` на symlink `../harness/claude/skills`.
  После шага `role-command` и прочие skills доступны через shell и harness path.

plan_contract:
  fr_ids: [FR-002, US-001, SC-001, SC-002]
  nouns: [".claude/skills", "harness/claude/skills", "role-command", "symlink"]
  layout_paths:
    - "harness/claude/skills/"
    - ".claude/skills"
  ac_quotes:
    - ".claude/skills — symlink; readlink -f → …/harness/claude/skills"
    - "harness/claude/skills/role-command — SoT"
  plan_jumps:
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md:103-103"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md:163-202"

context:
  consumes:
    - "s01 done (harness/claude/ directory exists)"
  produces:
    - "harness/claude/skills/ (SoT directory, moved from .claude/skills)"
    - ".claude/skills -> ../harness/claude/skills (symlink)"
  plan_refs:
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §FR-002"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §US-001"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §SC-001 SC-002"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §Target layout"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §0.11 counterparts"
  files:
    - "harness/claude/skills/"
    - ".claude/skills"

as_built:
  - ".claude/skills — real directory; содержит role-command/ и другие skill dirs"
  - "harness/claude/ — существует (s01/s046)"

delta:
  - "git mv .claude/skills harness/claude/skills"
  - "rm -rf .claude/skills (если остался как real dir)"
  - "ln -s ../harness/claude/skills .claude/skills"

deletes:
  - ".claude/skills (real dir — replaced by symlink; content moved to harness/claude/skills)"

out_of_scope:
  - "s01: commands mv (done)"
  - "s03: .claude/rules mv (следующий шаг)"
  - "full pytest suite (s07)"

skills:
  code_surface: shell
  impl:
    - tdd
    - python-testing-patterns
    - modern-python
    - python-anti-patterns

checkpoints:
  - id: cp1
    criterion: harness/claude/skills существует; role-command subdir внутри
    verify: "test -d harness/claude/skills/role-command"
  - id: cp2
    criterion: .claude/skills — symlink, разрешается в harness/claude/skills
    verify: "test -L .claude/skills && readlink -f .claude/skills | grep harness/claude/skills"
  - id: cp3
    criterion: нет dual SoT — .claude/skills не real dir
    verify: "test ! -d .claude/skills -o -L .claude/skills"
  - id: cp4
    criterion: rg — нет import/references на .claude/skills как real SoT
    verify: "rg -l 'mkdir.*\\.claude/skills' bin/ loop/ harness/ || true"

verify: []
tdd:
  - "loop/tests/test_harness_paths.py: assert test -L .claude/skills + readlink → harness/claude/skills"
