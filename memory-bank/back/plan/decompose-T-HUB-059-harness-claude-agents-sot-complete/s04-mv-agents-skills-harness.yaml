schema: epic-decompose/v1
role: back
step_id: s04
plan_id: T-HUB-059-harness-claude-agents-sot-complete
title: "git mv .agents/skills → harness/skills + symlink (SoT agent skills)"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Перенести `.agents/skills` (~19M, ~1485 skills) в `harness/skills` как единственный SoT;
  заменить `.agents/skills` на symlink `../harness/skills`.
  `.agents/.skill-lock.json` и прочие non-skills файлы в `.agents/` остаются real files — не требовать symlink всего `.agents`.
  После шага Cursor/agents consumers читают skills через обе пути (inode один).

plan_contract:
  fr_ids: [FR-004, US-002, SC-001, SC-002]
  nouns: [".agents/skills", "harness/skills", "symlink", ".skill-lock.json"]
  layout_paths:
    - "harness/skills/"
    - ".agents/skills"
  ac_quotes:
    - "test -L .agents/skills && readlink -f → …/harness/skills"
    - "sample SKILL.md открывается через обе пути; inode/target один SoT (harness/skills)"
    - ".agents/.skill-lock.json остаётся в .agents/ как real file"
  plan_jumps:
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md:105-105"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md:163-202"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md:127-128"

context:
  consumes:
    - "s01–s03 done (harness/claude/ complete)"
  produces:
    - "harness/skills/ (SoT directory, moved from .agents/skills)"
    - ".agents/skills -> ../harness/skills (symlink)"
  plan_refs:
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §FR-004"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §US-002"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §SC-001 SC-002"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §Target layout"
    - "plan-T-HUB-059-harness-claude-agents-sot-complete.md §Assumptions"
  files:
    - "harness/skills/"
    - ".agents/skills"
    - ".agents/.skill-lock.json"

as_built:
  - ".agents/skills — real directory (~19M); второй editable SoT для agent skills"
  - ".agents/.skill-lock.json — real file; остаётся после mv"
  - "harness/ — существует (041/046)"

delta:
  - "git mv .agents/skills harness/skills"
  - "rm -rf .agents/skills (если остался как real dir после mv)"
  - "ln -s ../harness/skills .agents/skills"
  - ".agents/.skill-lock.json — не трогать"

deletes:
  - ".agents/skills (real dir — replaced by symlink; content moved to harness/skills)"

out_of_scope:
  - ".agents/.skill-lock.json — не SoT, остаётся real"
  - ".agents/ целиком — не symlink (только .agents/skills — symlink)"
  - "full pytest suite (s07)"

skills:
  code_surface: shell
  impl:
    - tdd
    - python-testing-patterns
    - modern-python
    - python-anti-patterns

checkpoints:
  - id: cp1
    criterion: harness/skills существует; sample SKILL.md доступен
    verify: "test -d harness/skills && find harness/skills -name 'SKILL.md' | head -1"
  - id: cp2
    criterion: .agents/skills — symlink, разрешается в harness/skills
    verify: "test -L .agents/skills && readlink -f .agents/skills | grep harness/skills"
  - id: cp3
    criterion: .agents/.skill-lock.json — real file, не затронут
    verify: "test -f .agents/.skill-lock.json && test ! -L .agents/.skill-lock.json"
  - id: cp4
    criterion: rg — нет мест, создающих .agents/skills как real dir (AC−2 check)
    verify: "rg -l 'mkdir.*\\.agents/skills' bin/ loop/ harness/ || true"

verify: []
tdd:
  - "loop/tests/test_harness_paths.py: assert test -L .agents/skills + readlink → harness/skills"
  - "loop/tests/test_harness_paths.py: sample SKILL.md reachable via both paths"
