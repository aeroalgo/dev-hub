schema: epic-decompose/v1
role: back
step_id: s03
plan_id: T-HUB-021
title: "make_output_cap_agent() + run_log_summary() sync runner with retries/fallback"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Расширить `llm_structured.py`: `make_output_cap_agent()` создаёт pydantic-ai Agent[LogSummary]
  через OpenAIProvider с base_url/api_key из `_lib.load_output_summary_env`;
  `run_log_summary(cmd, sample, dump_path) -> LogSummary | None` — синхронный wrapper (asyncio.run),
  timeout, retries + fallback model chain; fail-soft: при любом исключении возвращает None.
  FR-002–FR-004, AC+5, AC+6.

context:
  consumes:
    - ".claude/hooks/llm_structured.py — LogError, LogSummary (из s02)"
    - ".claude/hooks/_lib.py — load_output_summary_env() env contract"
    - ".claude/project.env — PROJECT_OUTPUT_SUMMARY_URL/MODEL/FALLBACK_MODEL/TIMEOUT/RETRIES/KEY"
    - "plan/T-HUB-021-pydantic-ai-output-cap/md/plan.md §Env contract + §Архитектура"
  produces:
    - ".claude/hooks/llm_structured.py — make_output_cap_agent(), run_log_summary()"
  plan_refs:
    - "plan/T-HUB-021-pydantic-ai-output-cap/md/plan.md FR-002"
    - "plan/T-HUB-021-pydantic-ai-output-cap/md/plan.md FR-003"
    - "plan/T-HUB-021-pydantic-ai-output-cap/md/plan.md FR-004"
    - "plan/T-HUB-021-pydantic-ai-output-cap/md/plan.md AC+5 (fallback chain)"
    - "plan/T-HUB-021-pydantic-ai-output-cap/md/plan.md AC+6 (hook never raises)"
  files:
    - ".claude/hooks/llm_structured.py"

as_built:
  - ".claude/hooks/llm_structured.py — только LogError + LogSummary (после s02)"
  - ".claude/hooks/bash-output-cap.py — _http_chat() urllib free-text path (старый; удаляется в s06)"
  - ".claude/hooks/_lib.py — load_output_summary_env() уже содержит все нужные ключи"

delta:
  - ".claude/hooks/llm_structured.py ADD — def make_output_cap_agent(env: dict) -> Agent[LogSummary]: OpenAIProvider(base_url, api_key), primary model, result_type=LogSummary"
  - ".claude/hooks/llm_structured.py ADD — def run_log_summary(cmd, sample, dump_path, *, env=None) -> LogSummary | None: asyncio.run, timeout, retries, fallback model chain, ValidationError retry, any Exception → None"
  - ".claude/hooks/llm_structured.py ADD — __all__ обновить: добавить 'make_output_cap_agent', 'run_log_summary'"

deletes: []

out_of_scope:
  - "bash-output-cap.py интеграция — s04"
  - "Удаление _http_chat / llm_summarize — s06 (legacy path остаётся до green suite)"
  - "Тестовый файл — s05"

skills:
  code_surface: python
  impl:
    - async-python-patterns
    - python-testing-patterns

checkpoints:
  - id: cp1
    criterion: "run_log_summary и make_output_cap_agent importable без ошибок"
    verify: "python3 -c \"import sys; sys.path.insert(0,'/home/aero/PyProject/dev-hub/.claude/hooks'); from llm_structured import run_log_summary, make_output_cap_agent; print('ok')\""
  - id: cp2
    criterion: "test_run_log_summary_returns_none_on_network_fail PASS (mock → ConnectionError)"
    verify: ".venv/bin/pytest /home/aero/PyProject/dev-hub/loop/tests/test_llm_structured.py::test_run_log_summary_returns_none_on_network_fail -q --tb=short 2>&1 | tail -5"
  - id: cp3
    criterion: "test_run_log_summary_falls_back_to_fallback_model PASS"
    verify: ".venv/bin/pytest /home/aero/PyProject/dev-hub/loop/tests/test_llm_structured.py::test_run_log_summary_falls_back_to_fallback_model -q --tb=short 2>&1 | tail -5"

verify: []

tdd:
  - "Red: test_run_log_summary_returns_none_on_network_fail — mock agent.run() raises ConnectionError → None"
  - "Red: test_run_log_summary_falls_back_to_fallback_model — primary raises, fallback returns valid LogSummary"
  - "Red: test_run_log_summary_retries_on_validation_error — first call ValidationError, second returns valid"
  - "Red: test_make_output_cap_agent_uses_env_url — mock OpenAIProvider → assert base_url from env"
  - "Green: реализовать make_output_cap_agent + run_log_summary"
  - "Тесты в loop/tests/test_llm_structured.py"
