schema: epic-decompose/v1
role: back
step_id: s05
plan_id: T-HUB-075-stack-profiles-rust-python-js
title: "Consumer-facing API integration — resolve_capability only, no lifecycle execution"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Consumer contract: workflow/skill authors call `resolve_capability()` only and
  receive the same JSON-ready CapabilityResolution as CLI. Independent Test
  fixture (tmp monorepo Python+Rust+JS) asserts exact test.full JSON per profile
  plus negatives (no default, second JS lockfile, root escape). Hub self-test
  canon (`bin/pytest`, `test_run_canon.py`, `tier1_verify.py`, `loop/tests/**`)
  unchanged — no migration through target profiles. No phase executes argv.
  Outcome: capability request is the consumer API, не «модуль импортируется».

plan_contract:
  fr_ids:
    - FR-007
    - FR-008
    - FR-009
    - FR-010
    - FR-011
    - FR-014
    - FR-015
    - US-001
    - US-002
    - US-003
    - US-004
    - US-005
    - SC-001
    - SC-003
    - SC-004
    - AC+2
    - AC+5
    - AC+8
    - AC−1
    - AC−4
    - AC−6
    - NFR-1
    - NFR-7
    - TM-005
    - TM-006
    - TM-007
    - TM-008
    - TM-009
    - TM-014
  nouns:
    - resolve_capability
    - CapabilityResolution
    - Independent Test monorepo
    - bin/pytest
    - test_run_canon
    - tier1_verify
    - hub self-test
  layout_paths:
    - loop/tests/test_stack_profiles_integration.py
    - loop/stack_profiles/resolver.py
    - harness/hooks/test_run_canon.py
    - loop/incidents/tier1_verify.py
  ac_quotes:
    - "Consumers use resolve_capability() only. Wiring an actual phase to execute commands is a future epic that declares its own capability."
    - "bin/pytest, harness/hooks/test_run_canon.py, loop/incidents/tier1_verify.py, loop/tests/** remain hub self-test; no migration through target profiles."
    - "API and CLI expose same machine result, never prose parsing."
    - "Create a temporary monorepo with Python, Rust and JS named targets. Resolve test.full for each and assert exact JSON."
    - "No change to hub-only pytest canon disguised as stack support."
  plan_jumps:
    - "memory-bank/back/plan/T-HUB-075-stack-profiles-rust-python-js/md/plan.md:99-103"
    - "memory-bank/back/plan/T-HUB-075-stack-profiles-rust-python-js/md/plan.md:183-197"
    - "memory-bank/back/plan/T-HUB-075-stack-profiles-rust-python-js/md/plan.md:339-341"
    - "memory-bank/back/plan/T-HUB-075-stack-profiles-rust-python-js/md/plan.md:279-301"

context:
  consumes:
    - s02 resolve_capability argv matrix
    - s03 CLI JSON parity
    - plan §Independent Test
    - plan §FR-014/015
  produces:
    - loop/tests/test_stack_profiles_integration.py
    - documented consumer import path
    - unchanged hub self-test files (lock)
  plan_refs:
    - plan.md#independent-test
    - plan.md#functional-requirements
    - plan.md#qa-consumes-test-plan
  files:
    - loop/tests/test_stack_profiles_integration.py
    - loop/stack_profiles/__init__.py
    - harness/hooks/test_run_canon.py
    - loop/incidents/tier1_verify.py

as_built:
  - "s02/s03 provide API+CLI"
  - "harness/hooks/test_run_canon.py prefixes: bin/pytest, .venv/bin/pytest, npm vitest/tsc"
  - "loop/incidents/tier1_verify.py maps diagnostics to loop/ pytest commands"
  - "loop/formula_render.py default verify .venv/bin/pytest -q — hub self-test keep (plan sunset A n/a)"
  - "no pack phase executes stack argv (Appetite cut)"

delta:
  - "integration test: tmp monorepo three named targets; resolve test.full each; exact argv/cwd/profile JSON"
  - "same fixture: drop default_target → target_selection_required; add second JS lockfile → js_package_manager_ambiguous; escape root → target_root_unsafe; all empty argv distinct codes"
  - "export public functions from loop.stack_profiles"
  - "assert CLI subprocess JSON == API for one fixture row (US-005)"
  - "lock tests: test_run_canon prefixes and tier1_verify commands unchanged (no cargo/npm as hub self-test)"
  - "rg: no subprocess.run of resolved argv in loop/workflow or stack_profiles (NFR-1 / FR-014)"
  - "Kind I: if generic consumer docs embed pytest/cargo/npm as the way to request quality ops, rewrite to capability names"

deletes: []

out_of_scope:
  - "leftover inventory scan (s06)"
  - "executing cargo/pytest/npm for the managed project"
  - "pack phase declaration syntax — Appetite cut_list"
  - "migrating formula_render / tier1_verify onto stack profiles — keep hub self-test"
  - "full suite → QA"

skills:
  code_surface: test
  impl:
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/python-testing-patterns/SKILL.md
    - .agents/skills/modern-python/SKILL.md
    - .agents/skills/python-anti-patterns/SKILL.md
    - .agents/skills/python-error-handling/SKILL.md
    - .agents/skills/python-type-safety/SKILL.md

checkpoints:
  - id: cp1
    criterion: |
      Independent Test PASS / SC-001 / TM-005/006/007 — tmp monorepo Python+Rust+JS
      resolve test.full exact JSON argv/cwd/profile. YAML/classes alone is not evidence.
    verify: "bin/pytest loop/tests/test_stack_profiles_integration.py -q --tb=line -k 'monorepo or test_full or python or rust or javascript'"
  - id: cp2
    criterion: |
      Independent Test negatives / SC-003/004 / TM-008 — remove default, second JS
      lockfile, root escape: distinct codes, argv [].
    verify: "bin/pytest loop/tests/test_stack_profiles_integration.py -q --tb=line -k 'selection or ambiguous or unsafe or escape'"
  - id: cp3
    criterion: |
      FR-014 / NFR-1 — no lifecycle execution: rg finds no subprocess of resolved
      argv / cargo test / npm run as stack-profile consumer in loop/workflow.
    verify: "rg -n 'resolve_capability\\(.*\\).*subprocess|subprocess.*resolve_capability' loop/workflow loop/stack_profiles || true"
  - id: cp4
    criterion: |
      FR-015 / TM-014 / AC+8 / AC−6 — test_run_canon prefixes and tier1_verify
      still hub pytest; formula_render default pytest kept. No disguise as stack support.
    verify: "bin/pytest harness/hooks/tests -q --tb=line -k 'test_run_canon' ; rg -n 'BIN_PYTEST_PREFIX|bin/pytest' harness/hooks/test_run_canon.py"

verify:
  - "bin/pytest loop/tests/test_stack_profiles_integration.py loop/tests/test_stack_profiles_resolver.py loop/tests/test_stack_profiles_cli.py -q --tb=line"
tdd:
  - "red: no monorepo Independent Test"
  - "green: API+CLI same JSON; hub self-test locked"
