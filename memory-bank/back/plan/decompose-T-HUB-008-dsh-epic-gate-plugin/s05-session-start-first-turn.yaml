schema: epic-decompose/v1
role: back
plan_id: T-HUB-008-dsh-epic-gate-plugin
step_id: s05
title: "SessionStart first-turn semantics — DSH bridge gap close"
next_phase: BACK IMPLEMENT
needs_creative: "no"
goal: "Закрыть или подтвердить no-op по SessionStart first-turn при DSH runtime без полного порта session-start.py в TS."

as_built:
  - "session-start.py: first-turn через state session_started"
  - "T-HUB-016 bridge SessionStart: событие транслируется; first-turn не исследован"
  - "dsh/README.md gap: SessionStart first-turn semantics — owner T-HUB-008"
delta:
  - "RESEARCH: DSH SessionStart payload — is_first_turn/first_message или аналог"
  - "DECISION: bridge-ok → no-op (документировать first-turn verdict: bridge-ok)"
  - "DECISION: gap-confirmed → EDIT session-start.py и/или thin epic-gate SessionStart handler"
  - "TEST: first-turn state корректен при DSH smoke"

research:
  dsh_contract:
    - "@deepseek-ai/dsh-agent emits agent/session-start exactly once before the first turn."
    - "Payload carries source=startup|resume|clear|compact; no is_first_turn or first_message field exists."
    - "@deepseek-ai/dsh-hooks-claude-code maps SessionStart additionalContext to agent.inject() at that lifecycle edge."
    - "The bridge does not support initialUserMessage or sessionTitle; neither is required by the existing Python hook contract."
  first-turn verdict: bridge-ok
  decision: "No product-code change: the existing Python SessionStart hook remains the source of truth and the DSH bridge delivers its additionalContext at the native first-turn lifecycle edge."
deletes:
  - n/a
out_of_scope:
  - "Полная реимплементация session-start.py в TS"
  - "gate-check-turn.py (s06)"

requirements_covered:
  - FR-007
  - NFR-4

context:
  files:
    - ".claude/hooks/session-start.py"
    - "dsh/plugins/epic-gate/"
    - "loop/tests/test_dsh_epic_gate_gaps.py"
    - "dsh/README.md"

skills:
  code_surface: service
  impl:
    - ".agents/skills/tdd/SKILL.md"
    - ".agents/skills/python-testing-patterns/SKILL.md"
    - ".agents/skills/modern-python/SKILL.md"
    - ".agents/skills/python-anti-patterns/SKILL.md"

verify:
  - "`rg -n 'first-turn verdict:' memory-bank/back/plan/decompose-T-HUB-008-dsh-epic-gate-plugin/s05-session-start-first-turn.yaml`"
  - "`timeout 300s .venv/bin/pytest loop/tests/test_dsh_epic_gate_gaps.py -k session_start -q --tb=line`"

tdd:
  - "test: session_started set on first DSH session"
  - "test: session_started not reset on DSH resume"
  - "test: epic-gate SessionStart no-op when bridge already handles first-turn"

checkpoints:
  - id: cp1
    criterion: "DSH SessionStart first-turn задокументирован (bridge-ok|gap-confirmed)"
    verify: "`rg -n 'first-turn verdict: (bridge-ok|gap-confirmed)' memory-bank/back/plan/decompose-T-HUB-008-dsh-epic-gate-plugin/s05-session-start-first-turn.yaml`"
  - id: cp2
    criterion: "Если gap-confirmed — session-start/epic-gate обеспечивают корректный first-turn; тесты зелёные"
    verify: "`timeout 300s .venv/bin/pytest loop/tests/test_dsh_epic_gate_gaps.py -k session_start -q --tb=line`"

gaps:
  status: none
  resolved:
    - id: g1
      description: "DSH SessionStart first-turn payload — bridge-ok; source=startup|resume, no is_first_turn/first_message"
      resolved_at: cp1
      resolution: "Native agent/session-start is emitted exactly once before the first turn; the bridge injects additionalContext there."
      evidence:
        - "@deepseek-ai/dsh-agent runtime-types.d.ts: SessionStartSource and agent/session-start payload"
        - "@deepseek-ai/dsh-agent README.md: first startup injection point"
        - "@deepseek-ai/dsh-hooks-claude-code README: SessionStart additionalContext mapping and initialUserMessage limitation"
        - "loop/tests/test_dsh_epic_gate_gaps.py: DSH native SessionStart smoke for startup/resume"
