schema: epic-decompose/v1
role: back
step_id: s06
plan_id: T-HUB-002
title: "architecture SoT note + legacy-fallback-purge rg verification suite"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Добавить в `memory-bank/architecture/overview.md` (или index.md) одну строку-заметку:
  «SoT rules = `.cursor/rules/`; role-command SoT = `.claude/skills/role-command/SKILL.md`,
  зеркало → `.agents/skills/role-command/SKILL.md`». Затем провести итоговый rg-purge-sweep:
  проверить что все wording-заменители из s01–s05 применены, нет призраков старых live-путей,
  нет битых @-ссылок. Это единственный шаг с `deletes` = legacy-fallback-purge.

context:
  consumes:
    - "memory-bank/architecture/overview.md — текущее содержимое"
    - "memory-bank/architecture/index.md — текущее содержимое"
    - "Результаты s01–s05 (все изменения уже применены)"
  produces:
    - "memory-bank/architecture/overview.md (edit: добавить SoT note)"
    - "rg-suite отчёт (inline в checkpoint verify)"
  plan_refs:
    - "plan-T-HUB-002-canon-sync.md §AC+ #11, AC+ #12"
    - "plan-T-HUB-002-canon-sync.md §Файлы для правки: memory-bank/architecture/overview.md"
  files:
    - "memory-bank/architecture/overview.md"

as_built:
  - "memory-bank/architecture/ — существует (containers.md, data-flow.md, erd.md, frontend.md, index.md, overview.md, services.md)"
  - "memory-bank/architecture/overview.md — существует; содержимое требует аудита для SoT note"
  - "s01–s05 — wording replacements применены (зависимость от предыдущих шагов)"
  - "Нет записи о rules SoT / role-command SoT в architecture"

delta:
  - >
    memory-bank/architecture/overview.md: добавить раздел или строку в §Hub tooling или §Conventions:
    ```
    ## Rules & role-command SoT
    - Rules canonical source: `.cursor/rules/` (Cursor) / `.claude/rules/` (Claude Code)
    - role-command SoT: `.claude/skills/role-command/SKILL.md` (зеркало: `.agents/skills/role-command/SKILL.md`)
    - Обновлять `.agents` одновременно с `.claude` (не расходиться)
    ```
  - >
    rg legacy-fallback-purge: после apply s01–s05 провести sweep:
    1. rg "L1–L2 без spawn" CLAUDE.md → 0 hits (replaced by spawn-hard pointer)
    2. rg "@.cursor/rules/project_manager/" mainrule.mdc → 0 live-path hits (only archived rows)
    3. rg "@.cursor/rules/team_lead/" mainrule.mdc → 0 live-path hits
    4. rg "@.cursor/rules/content_growth/" mainrule.mdc → 0 live-path hits
    5. rg "@.cursor/rules/marketing_growth/" mainrule.mdc → 0 live-path hits
    6. rg "@.cursor/rules/seo_ops/" mainrule.mdc → 0 live-path hits
    7. rg "ARCHIVED\|FAIL" .claude/commands/pm-plan.md → ≥1 hit
    8. rg "N/A protocol\|Hub" .cursor/rules/graphify.mdc → ≥1 hit
    9. rg "INTEG PLAN\|Plan exceptions" .cursor/rules/graphify.mdc → ≥1 hit
    10. test -f .cursor/rules/front-tests-parent-only.mdc → exists

deletes:
  - "Все remaining wording-призраки из s01–s05 — rg-sweep выявляет, manual fix или re-apply шага"
  - "Любой broken @-ref выявленный sweep → fix inline или note as follow-up"

out_of_scope:
  - "Vendor _archive/ полностью — product-decision, не этот эпик"
  - "Cursor hooks epic parity — T-HUB-004+"

skills:
  code_surface: docs
  impl: []

checkpoints:
  - id: cp1
    criterion: "memory-bank/architecture/overview.md содержит SoT note о rules и role-command"
    verify: "grep -n 'SoT\\|role-command\\|mirror\\|.agents' /home/aero/PyProject/dev-hub/memory-bank/architecture/overview.md | head -10"
  - id: cp2
    criterion: "rg legacy purge: нет live project_manager/team_lead/content_growth/marketing_growth/seo_ops в mainrule без archived маркера"
    verify: |
      echo "=== mainrule live role paths ===" && \
      grep -n "project_manager\|team_lead\|content_growth\|marketing_growth\|seo_ops" \
        /home/aero/PyProject/dev-hub/.cursor/rules/mainrule.mdc | grep -v "archived\|FAIL\|#" || echo "OK: 0 live hits"
  - id: cp3
    criterion: "rg purge: нет 'L1–L2 без spawn' как абсолютной строки в CLAUDE.md"
    verify: "grep -c 'L1–L2 без spawn' /home/aero/PyProject/dev-hub/CLAUDE.md && echo FOUND || echo OK_REMOVED"
  - id: cp4
    criterion: "rg purge: .cursor/rules/front-tests-parent-only.mdc существует"
    verify: "test -f /home/aero/PyProject/dev-hub/.cursor/rules/front-tests-parent-only.mdc && echo OK || echo MISSING"
  - id: cp5
    criterion: "rg purge: graphify.mdc содержит hub N/A + INTEG PLAN exception"
    verify: |
      grep -c "N/A protocol\|Hub\|INTEG PLAN\|Plan exceptions" \
        /home/aero/PyProject/dev-hub/.cursor/rules/graphify.mdc
  - id: cp6
    criterion: "rg purge: pm-plan.md + seo-tech.md + tl-standup.md содержат ARCHIVED FAIL preamble"
    verify: |
      for f in /home/aero/PyProject/dev-hub/.claude/commands/pm-plan.md \
                /home/aero/PyProject/dev-hub/.claude/commands/seo-tech.md \
                /home/aero/PyProject/dev-hub/.claude/commands/tl-standup.md; do
        grep -q 'ARCHIVED\|FAIL' "$f" && echo "OK: $f" || echo "MISSING: $f"
      done

verify: []
tdd: []
