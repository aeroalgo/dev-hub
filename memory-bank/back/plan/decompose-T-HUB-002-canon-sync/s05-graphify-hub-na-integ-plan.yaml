schema: epic-decompose/v1
role: back
step_id: s05
plan_id: T-HUB-002
title: "graphify.mdc hub N/A protocol + INTEG PLAN inventory exception in graphify + mainrule Step 0"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Добавить в `graphify.mdc` две секции: (1) hub/tooling N/A protocol — что делать агенту когда
  `graphify-out/graph.json` отсутствует (hub не product; документировать попытку, fallback inventory,
  не halt); (2) exception allowlist: INTEG PLAN + brownfield VAN = document attempt + inventory fallback
  (не halt как будто CLI есть). Обновить mainrule.mdc Step 0 graphify строку — добавить explicit
  allowlist exceptions (INTEG PLAN / brownfield VAN → inventory mode).

context:
  consumes:
    - ".cursor/rules/graphify.mdc — текущее содержимое (без hub N/A section)"
    - ".cursor/rules/mainrule.mdc — Step 0 graphify строка"
    - "plan-T-HUB-002-canon-sync.md §FR-9, AC+ #9, AC+ #10, AC− #5"
  produces:
    - ".cursor/rules/graphify.mdc (edit: добавить ## Hub/tooling N/A protocol + ## Plan exceptions)"
    - ".cursor/rules/mainrule.mdc (edit: Step 0 — уточнить wording INTEG PLAN + brownfield VAN)"
  plan_refs:
    - "plan-T-HUB-002-canon-sync.md §FR-9"
    - "plan-T-HUB-002-canon-sync.md §AC+ #9, AC+ #10, AC− #5"
    - "plan-T-HUB-002-canon-sync.md §Зафиксированные решения (graphify exception пояснено)"
  files:
    - ".cursor/rules/graphify.mdc"
    - ".cursor/rules/mainrule.mdc"

as_built:
  - ".cursor/rules/graphify.mdc — нет секции hub N/A protocol; нет INTEG PLAN exception"
  - ".cursor/rules/graphify.mdc — содержит HARD RULE cwd + allowlist IMPLEMENT/TASK/BUGFIX/QA/SECURITY/brownfield VAN + skip list (PLAN, DECOMPOSE, CREATIVE, ...)"
  - ".cursor/rules/graphify.mdc:last — '## What is NOT in the graph' — не mention hub/tooling N/A"
  - ".cursor/rules/mainrule.mdc Step 0 graphify — строка описывает skip list, но нет явного 'INTEG PLAN → inventory mode' и нет 'hub → N/A protocol'"
  - "Риск: агент пытается graphify на hub → нет graph.json → halt вместо fallback"

delta:
  - >
    graphify.mdc: добавить секцию ## Hub / tooling repositories (N/A protocol):
    - Если `graphify-out/graph.json` не существует → не halt
    - Документировать попытку в лог/chat: «graphify N/A — hub repository»
    - Fallback: использовать inventory (ls/rg по memory-bank/, .cursor/rules/, .claude/)
    - Не блокировать workflow; inventory достаточен для docs-only шагов
  - >
    graphify.mdc: добавить секцию ## Plan exceptions (inventory mode):
    - INTEG PLAN → не запускать `graphify query`; вместо этого читать inventory
      из соответствующего `workflow-integ-plan.mdc` (portal routes + UI elements)
    - brownfield VAN → graphify ОК, но если N/A → inventory из architecture/**
    - Allowlist явный: эти два режима = «document attempt + inventory fallback», не halt
  - >
    mainrule.mdc Step 0: добавить к skip list или clarify note:
    «Hub repositories без graph.json: N/A protocol (не halt) — @.cursor/rules/graphify.mdc §Hub N/A»
    «INTEG PLAN: inventory mode — @.cursor/rules/graphify.mdc §Plan exceptions»

deletes:
  - "graphify.mdc: строка 'Do not skip graphify because files are already known or because a plan is being executed' — требует уточнения для INTEG PLAN (→ replaced by Plan exceptions section)"

out_of_scope:
  - "Изменения в CLAUDE.md spawn — s01"
  - "mainrule archived prefix table — s04"
  - "rg final purge — s06"

skills:
  code_surface: docs
  impl: []

checkpoints:
  - id: cp1
    criterion: "graphify.mdc содержит секцию 'Hub' или 'N/A protocol' (hub/tooling без graph.json)"
    verify: "grep -n 'Hub\\|N/A\\|tooling\\|protocol' /home/aero/PyProject/dev-hub/.cursor/rules/graphify.mdc | head -10"
  - id: cp2
    criterion: "graphify.mdc содержит 'INTEG PLAN' exception или 'Plan exceptions' секцию"
    verify: "grep -n 'INTEG PLAN\\|Plan exceptions\\|inventory' /home/aero/PyProject/dev-hub/.cursor/rules/graphify.mdc | head -10"
  - id: cp3
    criterion: "mainrule.mdc Step 0 содержит reference на hub N/A или INTEG PLAN exception"
    verify: "grep -n 'N/A\\|Hub\\|INTEG PLAN\\|inventory' /home/aero/PyProject/dev-hub/.cursor/rules/mainrule.mdc | head -10"
  - id: cp4
    criterion: "Проблемная строка 'Do not skip graphify because files are already known' дополнена или заменена уточнённым wording"
    verify: "grep -n 'Do not skip graphify' /home/aero/PyProject/dev-hub/.cursor/rules/graphify.mdc | head -5"

verify: []
tdd: []
