schema: epic-decompose/v1
role: back
plan_id: T-HUB-008-dsh-epic-gate-plugin
step_id: s06
title: "gate-check-turn.py — conditional turn-stopping (только если bridge Stop недостаточен)"
next_phase: BACK IMPLEMENT
needs_creative: "no"
goal: "Подтвердить достаточность DSH Stop/self-limit для FINISH-blocking либо добавить gate-check-turn.py только при красном интеграционном тесте."

as_built:
  - "stop-gate.py: Stop event; после T-HUB-016 DSH self-limit остановка поддерживается"
  - "gate-check-turn.py: не существует"
  - "T-HUB-016 s05: stop-gate DSH self-limit реализован"
delta:
  - "TEST: loop/tests/test_dsh_epic_gate_gaps.py::test_dsh_stop_finish_blocking"
  - "DECISION: тест зелёный → skip (no new files); красный → implement gate-check-turn.py"
  - "CONDITIONAL: .claude/hooks/gate-check-turn.py + mount в Stop hooks (DSH only)"
deletes:
  - n/a
out_of_scope:
  - "Полный порт stop-gate.py в TS"
  - "DSH self-limit (уже в T-HUB-016)"

requirements_covered:
  - FR-006

context:
  files:
    - ".claude/hooks/stop-gate.py"
    - ".claude/hooks/gate-check-turn.py"
    - "loop/tests/test_dsh_epic_gate_gaps.py"
    - ".claude/settings.json"

skills:
  code_surface: service
  impl:
    - ".agents/skills/tdd/SKILL.md"
    - ".agents/skills/python-testing-patterns/SKILL.md"
    - ".agents/skills/modern-python/SKILL.md"
    - ".agents/skills/python-anti-patterns/SKILL.md"

verify:
  - "`rg -n 'test_dsh_stop_finish_blocking' loop/tests/test_dsh_epic_gate_gaps.py`"
  - "`timeout 300s .venv/bin/pytest loop/tests/test_dsh_epic_gate_gaps.py -k stop -q --tb=line`"

tdd:
  - "test: stop-gate blocks FINISH when DSH self-limit not exceeded"
  - "test: stop-gate allows stop when DSH self-limit exceeded"
  - "test (conditional): gate-check-turn increments turn counter"

checkpoints:
  - id: cp1
    criterion: "Интеграционный тест stop-blocking написан"
    verify: "`rg -n 'test_dsh_stop_finish_blocking' loop/tests/test_dsh_epic_gate_gaps.py`"
  - id: cp2
    criterion: "Тест зелёный (bridge Stop достаточен) ИЛИ gate-check-turn.py создан при красном тесте"
    verify: "`timeout 300s .venv/bin/pytest loop/tests/test_dsh_epic_gate_gaps.py -k stop -q --tb=line`"

gaps:
  - id: g1
    description: "Может оказаться no-op если bridge Stop достаточен — решается интеграционным тестом"
    status: open
    resolves_at: cp2
