schema: epic-decompose/v1
role: back
step_id: s10
plan_id: T-HUB-023-hooks-llm-fallbacks
title: AUDIT proof — regex happy path zero LLM calls + flags-off regression
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  AUDIT-шаг: доказать FR-010 и SC-001/SC-004 — grep + pytest что regex/sidecar happy path
  не вызывает LLM; flags-off regression vs baseline agent_hooks subset без деградации.

context:
  consumes:
    - "s04, s05 — wire-in complete"
    - "s08 — test_hooks_llm_fallback.py"
    - "loop/tests/test_agent_hooks.py"
    - "plan-T-HUB-023 FR-010"
  produces:
    - "loop/tests/test_hooks_llm_audit.py — anti-fallback grep assertions"
    - "memory-bank/back/plan/refs/T-HUB-023-audit-zero-llm.md — audit evidence log"
  plan_refs:
    - "plan-T-HUB-023 FR-010"
    - "plan-T-HUB-023 SC-001"
    - "plan-T-HUB-023 SC-004"
    - "plan-T-HUB-023 §TDD plan #5"
  files:
    - "loop/tests/test_hooks_llm_audit.py"
    - "memory-bank/back/plan/refs/T-HUB-023-audit-zero-llm.md"

as_built:
  - "No dedicated audit for LLM call count on happy path"

delta:
  - "CREATE test_hooks_llm_audit.py: golden inputs with regex hit → patch run_*_extract → assert not called"
  - "Run test_agent_hooks subset with PROJECT_HOOKS_LLM_*=0 — compare pass count"
  - "rg audit: extract_verdict must not call run_verdict_extract before regex attempt"
  - "rg audit: extract_handoff_block must call llm only after regex empty"
  - "CREATE T-HUB-023-audit-zero-llm.md with grep outputs + pytest summary"

deletes: []

out_of_scope:
  - "Legacy purge — greenfield, no deletes"
  - "Live LLM integration test"

skills:
  code_surface: test
  impl:
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/python-testing-patterns/SKILL.md
    - .agents/skills/modern-python/SKILL.md
    - .agents/skills/python-anti-patterns/SKILL.md

checkpoints:
  - id: cp1
    criterion: "grep: run_verdict_extract not at module import time in _lib"
    verify: "rg 'run_verdict_extract' .claude/hooks/_lib.py"
  - id: cp2
    criterion: "Happy path golden: LLM runners not invoked"
    verify: ".venv/bin/pytest loop/tests/test_hooks_llm_audit.py -q"
  - id: cp3
    criterion: "test_agent_hooks subset green with flags off"
    verify: ".venv/bin/pytest loop/tests/test_agent_hooks.py -q"
  - id: cp4
    criterion: "Audit evidence md exists"
    verify: "test -f memory-bank/back/plan/refs/T-HUB-023-audit-zero-llm.md"

verify: []

tdd:
  - "test_happy_path_handoff_no_llm"
  - "test_happy_path_verdict_sidecar_no_llm"
  - "test_happy_path_verdict_regex_no_llm"
  - "test_agent_hooks_regression_flags_off"
