schema: epic-decompose/v1
role: back
step_id: s09
plan_id: T-HUB-023-hooks-llm-fallbacks
title: Operator docs — project.env flags + loop/README reliability section
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Документировать когда и как включать LLM fallbacks: флаги в project.env, trigger order,
  что НЕ заменяется (spawn-hard), рекомендации для flash/noisy models (FR-009, AC+7, AC−3).

context:
  consumes:
    - "s01 — env contract"
    - "plan-T-HUB-023 §Prompt constraints, §Зафиксированные решения"
    - "loop/README.md — existing reliability sections"
  produces:
    - "loop/README.md — § reliability / hooks LLM fallback subsection"
    - ".claude/project.env — finalized comments (if not done in s01)"
  plan_refs:
    - "plan-T-HUB-023 FR-009"
    - "plan-T-HUB-023 AC+ #7"
    - "plan-T-HUB-023 AC− #1, #3"
  files:
    - "loop/README.md"
    - ".claude/project.env"

as_built:
  - "loop/README.md — no hooks LLM fallback section"
  - "project.env — may have stub from s01"

delta:
  - "README ADD subsection: PROJECT_HOOKS_LLM_* flags table, default-off rationale, trigger order diagram"
  - "README: when to enable (flash models, long transcripts, fingerprint_stall ops)"
  - "README: explicit — does NOT replace spawn-hard VERDICT protocol"
  - "project.env: ensure all flags documented inline with defaults 0"

deletes: []

out_of_scope:
  - "Code changes"
  - "Claude spawn-hard.md edits — AC−3"

skills:
  code_surface: infra
  impl: []

checkpoints:
  - id: cp1
    criterion: "README contains PROJECT_HOOKS_LLM_FALLBACK"
    verify: "rg 'PROJECT_HOOKS_LLM_FALLBACK' loop/README.md"
  - id: cp2
    criterion: "README mentions trigger order typed→regex→LLM"
    verify: "rg 'regex.*LLM|typed.*regex' loop/README.md"
  - id: cp3
    criterion: "README states spawn-hard not replaced"
    verify: "rg 'spawn-hard' loop/README.md"

verify: []

tdd: []
