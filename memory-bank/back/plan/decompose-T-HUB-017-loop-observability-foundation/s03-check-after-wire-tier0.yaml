schema: epic-decompose/v1
role: back
plan_id: T-HUB-017-loop-observability-foundation
step_id: s03
title: "Loop продолжается после Tier-0 — wire tier0 в check_after + repair_exhausted"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  После halt от check_after tier0 pipeline открывает incident, пытается repair и при успехе
  возвращает ok:true continue — loop не останавливается на repairable desync (US-002, SC-002).
  repair_exhausted пробрасывается в halt_logic как сейчас.

as_built:
  - "check_after в context_loop.py вызывает inline repair_index_mirror, repair_fingerprint_stall после finish_integrity"
  - "halt_logic.decide_after_action уже обрабатывает repair_exhausted"
  - "tier0/store из s01–s02 существуют но не wired в check_after"
delta:
  - "EDIT loop/context_loop.py check_after — после существующих inline repairs, перед return halt:"
  - "  1) собрать diagnostic_codes из finish_integrity / shape / stall payloads"
  - "  2) если halt imminent (ok:false) → open_incident(...) через store"
  - "  3) run_tier0_for_incident для matching open incidents"
  - "  4) если все resolved → return {ok:true, continue:true, incidents_resolved:[...]}"
  - "  5) если repair_exhausted → return halt с repair_exhausted:true (existing halt_logic contract)"
  - "Dual-path: inline repairs остаются в первой итерации (plan sunset greenfield); tier0 оборачивает, не заменяет"
  - "ADD loop/tests/test_check_after_tier0_wire.py — integration fixture desync → tier0 → continue"
  - "Regression: test_decide_after_action, test_status_* остаются green (AC+ #10)"
deletes: []
out_of_scope:
  - "metrics increment on tier0 (s05)"
  - "trace append (s04)"
  - "event emission (s08)"
  - "legacy inline repair dedup (optional follow-up — не в scope 017)"

context:
  consumes:
    - "s01 store"
    - "s02 tier0"
    - "plan §Tier-0 integration in check_after"
    - "plan FR-005"
  produces:
    - "check_after tier0 wire in context_loop.py"
  plan_refs:
    - "plan-T-HUB-017 §Tier-0 integration in check_after"
    - "plan-T-HUB-017 FR-005"
  files:
    - "loop/context_loop.py"
    - "loop/tests/test_check_after_tier0_wire.py"

skills:
  code_surface: service
  impl:
    - ".agents/skills/tdd/SKILL.md"
    - ".agents/skills/python-testing-patterns/SKILL.md"
    - ".agents/skills/modern-python/SKILL.md"
    - ".agents/skills/python-anti-patterns/SKILL.md"
    - ".agents/skills/python-error-handling/SKILL.md"
    - ".agents/skills/python-observability/SKILL.md"

checkpoints:
  - id: cp1
    criterion: "check_after halt + repairable diagnostic → open_incident вызван"
    verify: "`.venv/bin/pytest loop/tests/test_check_after_tier0_wire.py -k opens_incident -q --tb=line`"
  - id: cp2
    criterion: "tier0 resolves → check_after returns ok:true continue with incidents_resolved"
    verify: "`.venv/bin/pytest loop/tests/test_check_after_tier0_wire.py -k auto_continue -q --tb=line`"
  - id: cp3
    criterion: "repair_exhausted → halt payload с repair_exhausted:true"
    verify: "`.venv/bin/pytest loop/tests/test_check_after_tier0_wire.py -k repair_exhausted -q --tb=line`"
  - id: cp4
    criterion: "Regression: test_decide_after_action + existing check_after tests green"
    verify: "`.venv/bin/pytest loop/tests/test_decide_after_action.py loop/tests/test_index_fail_closed.py -q --tb=line`"

verify:
  - "`.venv/bin/pytest loop/tests/test_check_after_tier0_wire.py -q --tb=line`"
  - "`.venv/bin/pytest loop/tests/test_decide_after_action.py -q --tb=line`"

tdd:
  - "test_check_after_opens_incident_on_halt"
  - "test_check_after_tier0_resolved_returns_continue"
  - "test_check_after_tier0_exhausted_halts_with_flag"
  - "test_check_after_non_repairable_halt_unchanged"

requirements_covered:
  - FR-005
  - "AC+ #7"
  - "AC+ #10"
  - SC-002
  - "US-002 integration"
