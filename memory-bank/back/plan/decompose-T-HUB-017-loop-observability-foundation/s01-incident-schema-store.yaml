schema: epic-decompose/v1
role: back
plan_id: T-HUB-017-loop-observability-foundation
step_id: s01
title: "Видимые orchestration-инциденты — schema loop-incident/v1 + incidents.jsonl store"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Разработчик видит открытые orchestration-инциденты после сбоя loop без чтения сырого stdout (US-001).
  Machine-readable incident log в runtime epic slot с fail-closed при corrupt JSONL (AC−6).

as_built:
  - "loop/incidents/ package не существует"
  - "incidents.jsonl не пишется; diagnostic_codes только в check_after return payload"
  - "epic_dir() в loop.sh / context_loop уже резолвит runtime/<slug>/epic/ — reuse, не invent второй каталог"
delta:
  - "ADD loop/incidents/__init__.py — package export: schema types + store API"
  - "ADD loop/incidents/schema.py — dataclasses/Pydantic для loop-incident/v1: incident_id (sha256 canonical fields), status open|resolved|escalated, opened_at, resolved_at, project_root, epic_id, step_id, phase, session_id, source, diagnostic_codes[], fingerprint, tier0_attempts, tier0_repair_log[], resolution_tier, resolution_action, runbook_rel, metadata"
  - "ADD loop/incidents/store.py — append_incident(epic_dir, incident), resolve_incident(epic_dir, incident_id, resolution), list_open_incidents(epic_dir), parse_incidents_jsonl(path) с fail-closed на corrupt line"
  - "FR-013 idempotency: reopen same diagnostic_code+fingerprint+session → update existing open incident (tier0_attempts bump), не duplicate flood"
  - "incident_id = sha256(canonical JSON subset: project_root, epic_id, step_id, session_id, diagnostic_codes sorted, fingerprint)"
  - "ADD loop/tests/test_incidents_schema_store.py — round-trip + idempotency + corrupt line"
  - "ADD loop/tests/fixtures/incidents/corrupt_incidents.jsonl для doctor/status тестов позже"
deletes: []
out_of_scope:
  - "tier0 execution (s02)"
  - "check_after wire (s03)"
  - "metrics/trace (s04–s06)"
  - "event emission (s08)"

context:
  consumes:
    - "plan §Incident schema (loop-incident/v1)"
    - "plan FR-001, FR-002, FR-013"
  produces:
    - "loop/incidents/schema.py"
    - "loop/incidents/store.py"
    - "runtime/<slug>/epic/incidents.jsonl (runtime artifact)"
  plan_refs:
    - "plan-T-HUB-017 §Incident schema"
    - "plan-T-HUB-017 FR-001 FR-002 FR-013"
  files:
    - "loop/incidents/__init__.py"
    - "loop/incidents/schema.py"
    - "loop/incidents/store.py"
    - "loop/tests/test_incidents_schema_store.py"
    - "loop/tests/fixtures/incidents/corrupt_incidents.jsonl"

skills:
  code_surface: service
  impl:
    - ".agents/skills/tdd/SKILL.md"
    - ".agents/skills/python-testing-patterns/SKILL.md"
    - ".agents/skills/modern-python/SKILL.md"
    - ".agents/skills/python-anti-patterns/SKILL.md"
    - ".agents/skills/python-type-safety/SKILL.md"  # schema validate/serialize
    - ".agents/skills/python-error-handling/SKILL.md"  # fail-closed corrupt JSONL
    - ".agents/skills/python-observability/SKILL.md"  # incident log contract

checkpoints:
  - id: cp1
    criterion: "loop-incident/v1 round-trip parse → serialize → parse без потери полей"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_schema_store.py -k round_trip -q --tb=line`"
  - id: cp2
    criterion: "append_incident + list_open_incidents возвращает open; resolve меняет status + resolution fields"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_schema_store.py -k store_resolve -q --tb=line`"
  - id: cp3
    criterion: "idempotency: повторный open с тем же fingerprint+session обновляет incident, не создаёт дубликат"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_schema_store.py -k idempotency -q --tb=line`"
  - id: cp4
    criterion: "corrupt JSONL line → parse fail-closed с понятной ошибкой (не silent skip)"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_schema_store.py -k corrupt -q --tb=line`"

verify:
  - "`.venv/bin/pytest loop/tests/test_incidents_schema_store.py -q --tb=line`"

tdd:
  - "test_incident_v1_round_trip_all_fields"
  - "test_incident_id_stable_sha256"
  - "test_append_and_list_open"
  - "test_resolve_incident_sets_resolution_tier"
  - "test_idempotency_same_fingerprint_session_updates_not_duplicates"
  - "test_corrupt_jsonl_raises_or_returns_fail_closed"

requirements_covered:
  - FR-001
  - FR-002
  - FR-013
  - "AC+ #1"
  - "US-001 partial (store only; status in s06)"
  - "AC−6 partial (store parse fail-closed)"
