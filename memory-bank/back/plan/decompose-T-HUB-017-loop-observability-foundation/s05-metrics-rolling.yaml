schema: epic-decompose/v1
role: back
plan_id: T-HUB-017-loop-observability-foundation
step_id: s05
title: "Измеримый autopilot — metrics.json rolling counters + rates"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Метрики repair success rate и auto-continue rate в rolling window (default 7d) для оценки
  качества autopilot (US-005). EPIC_INCIDENT_METRICS=0 отключает запись (FR-012).

as_built:
  - "metrics.json не существует в epic runtime slot"
  - "нет счётчиков tier0_attempts / tier0_success / incidents_opened"
delta:
  - "ADD loop/incidents/metrics.py — schema loop-metrics/v1; counters: sessions_total, tier0_attempts, tier0_success, tier0_fail, incidents_opened, incidents_escalated, check_after_halt, check_after_continue; rates: tier0_success_rate, auto_continue_rate"
  - "increment_counter(epic_dir, name) + recompute_rates(); rolling window prune by window_days (default 7); atomic write (tmp + rename)"
  - "Wire increment hooks: вызывать из tier0.py (attempt/success/fail), check_after wire (halt/continue), open_incident (incidents_opened) — минимальные call sites в s03/s02 или здесь с thin import"
  - "FR-012: EPIC_INCIDENT_METRICS=0 → no-op"
  - "ADD loop/tests/test_incidents_metrics.py — increment, prune, rate computation fixture 2 success 1 fail → 0.67"
deletes: []
out_of_scope:
  - "status payload exposure (s06)"
  - "Prometheus / external TSDB (plan assumption: local-first)"

context:
  consumes:
    - "s02 tier0 call sites"
    - "s03 check_after wire"
    - "plan §Metrics schema"
    - "plan FR-007 FR-012"
  produces:
    - "loop/incidents/metrics.py"
    - "runtime/<slug>/epic/metrics.json"
  plan_refs:
    - "plan-T-HUB-017 §Metrics schema"
    - "plan-T-HUB-017 FR-007 FR-012"
  files:
    - "loop/incidents/metrics.py"
    - "loop/incidents/tier0.py"
    - "loop/context_loop.py"
    - "loop/tests/test_incidents_metrics.py"

skills:
  code_surface: service
  impl:
    - ".agents/skills/tdd/SKILL.md"
    - ".agents/skills/python-testing-patterns/SKILL.md"
    - ".agents/skills/modern-python/SKILL.md"
    - ".agents/skills/python-anti-patterns/SKILL.md"
    - ".agents/skills/python-configuration/SKILL.md"
    - ".agents/skills/python-observability/SKILL.md"

checkpoints:
  - id: cp1
    criterion: "increment tier0_success + tier0_attempts → tier0_success_rate вычисляется корректно"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_metrics.py -k success_rate -q --tb=line`"
  - id: cp2
    criterion: "rolling window prune удаляет записи старше window_days"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_metrics.py -k prune -q --tb=line`"
  - id: cp3
    criterion: "atomic write: metrics.json всегда валидный JSON после crash simulation"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_metrics.py -k atomic -q --tb=line`"
  - id: cp4
    criterion: "EPIC_INCIDENT_METRICS=0 → increment no-op"
    verify: "`.venv/bin/pytest loop/tests/test_incidents_metrics.py -k disabled -q --tb=line`"

verify:
  - "`.venv/bin/pytest loop/tests/test_incidents_metrics.py -q --tb=line`"

tdd:
  - "test_metrics_increment_counters"
  - "test_metrics_tier0_success_rate_two_of_three"
  - "test_metrics_rolling_window_prune"
  - "test_metrics_atomic_write"
  - "test_metrics_disabled_by_env"

requirements_covered:
  - FR-007
  - FR-012
  - "AC+ #4"
  - US-005
