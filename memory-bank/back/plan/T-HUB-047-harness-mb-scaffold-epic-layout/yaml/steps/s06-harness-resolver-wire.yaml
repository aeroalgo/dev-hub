schema: epic-decompose/v1
role: back
step_id: s06
plan_id: T-HUB-047-harness-mb-scaffold-epic-layout
title: Harness integration — epic_paths.py thin wrapper + _lib.py/reconcile.py/seed_implement/mb_finish
  resolver wire
next_phase: BACK IMPLEMENT
needs_creative: 'no'
goal: 'Перевести harness-зависимые модули (epic_paths.py, _lib.py, reconcile.py, seed_implement,

  mb_finish/*) на resolver из loop/paths/epic_layout.py.

  epic_paths.py становится thin wrapper (re-export + compat alias layout_v1_deprecated).

  mb_load (T-HUB-045): expose resolver API чтобы 045 мог consume когда ship.

  '
plan_contract:
  fr_ids:
  - FR-003
  - FR-012
  - FR-015
  - AC-2
  nouns:
  - harness/hooks/epic_paths.py thin wrapper
  - harness/hooks/_lib.py
  - harness/hooks/epic/reconcile.py
  - harness/hooks/epic/seed_implement (если существует)
  - loop/mb_finish/
  - loop/paths/epic_layout.resolve
  - layout_v1_deprecated diagnostic
  - compat alias
  layout_paths:
  - harness/hooks/epic_paths.py (rewire)
  - harness/hooks/_lib.py (rewire regex)
  - loop/mb_finish/ (rewire paths)
  ac_quotes:
  - 'AC-2: loop/paths/epic_layout.py resolver — единственный path source in loop+harness
    (post-purge)'
  - 'FR-012: harness integration: resolver; compat alias for legacy paths until migrate
    applied (diagnostic layout_v1_deprecated)'
  - 'FR-015: mb-load bundle paths via resolver when epic ships'
  plan_jumps:
  - plan/T-HUB-047-harness-mb-scaffold-epic-layout/md/plan.md:99-100
context:
  consumes:
  - loop/paths/epic_layout.py (s02)
  - harness/hooks/epic_paths.py (as-built v1 — rewrite)
  - harness/hooks/_lib.py
  - loop/mb_finish/
  produces:
  - harness/hooks/epic_paths.py (thin wrapper)
  - harness/hooks/_lib.py (updated)
  - loop/mb_finish/ (updated)
  plan_refs:
  - FR-003
  - FR-012
  - FR-015
  files:
  - harness/hooks/epic_paths.py
  - harness/hooks/_lib.py
  - harness/hooks/epic/core.py
  - harness/hooks/epic/reconcile.py
  - harness/hooks/session_resilience.py
  - loop/mb_finish/finish_implement.py
  - loop/mb_finish/finish_decompose.py
as_built:
- 'harness/hooks/epic_paths.py: glob ''decompose-*/index.yaml'', ''plan-*.md'' regex,
  f''decompose-{epic_id}'' — хардкодит v1 пути'
- 'harness/hooks/_lib.py: regex ''plan/decompose-|implement/implement-'' hardcoded'
- 'harness/hooks/epic/core.py: импортирует epic_paths функции; path construction через
  строки'
- 'harness/hooks/epic/reconcile.py: glob/path к decompose через epic_paths (indirect
  v1)'
- 'harness/hooks/session_resilience.py: ссылки на decompose/implement пути через строки'
- 'loop/mb_finish/: пути к decompose/implement yaml через строки, не resolver'
delta:
- 'Переписать harness/hooks/epic_paths.py: импортировать loop.paths.epic_layout.resolve;
  все функции возвращающие пути — через resolver; сохранить public API (function signatures
  unchanged); добавить compat alias с logging.warning(''layout_v1_deprecated'') для
  v1 paths'
- 'Обновить harness/hooks/_lib.py: заменить regex ''plan/decompose-'' + ''implement/implement-''
  на resolver calls или validation через EpicLayoutKind'
- 'Обновить harness/hooks/epic/core.py: заменить все path-строки через resolver; импорт
  epic_paths → resolver'
- 'Обновить harness/hooks/epic/reconcile.py: glob decompose index → resolve(role,
  epic_id, ''decompose_index_yaml''); явный resolver вызов'
- 'Обновить harness/hooks/session_resilience.py: пути decompose/implement → resolver'
- 'Обновить loop/mb_finish/finish_implement.py, finish_decompose.py: resolve(role,
  epic_id, ''decompose_step''/''implement_step'', step_id) вместо строк'
- Expose loop.paths.epic_layout.resolve в harness/hooks/__init__.py или epic_paths.py
  для mb-load T-HUB-045
deletes:
- harness/hooks/epic_paths.py — glob 'decompose-*/index.yaml' (строка 235)
- harness/hooks/epic_paths.py — glob 'decompose-*/index.md' (строка 239)
- harness/hooks/epic_paths.py — expected = ... / f'decompose-{epic_id}' / 'index.yaml'
  (строка 271)
- harness/hooks/epic_paths.py — plan_dir / f'decompose-{epic_id}' (строка 308)
- harness/hooks/epic_paths.py — plan_dir.glob(f'plan-{lookup_id}*.md') hardcoded (строка
  334-336)
- harness/hooks/_lib.py — regex 'plan/decompose-|implement/implement-'
out_of_scope:
- loop hot paths (context_loop, arm_phase) → s07
- purge (→ s11)
- mb-load full integration (T-HUB-045 — expose API only)
skills:
  code_surface: api
  impl:
  - modern-python
  - python-testing-patterns
checkpoints:
- id: cp1
  criterion: 'harness/hooks/epic_paths.py: rg ''decompose-'' epic_paths.py | grep
    -v ''layout_v1_deprecated\|#\|test'' → 0 строк hardcoded'
  verify: rg 'decompose-' harness/hooks/epic_paths.py | grep -v 'layout_v1_deprecated\|#\|test\|deprecated'
    | wc -l | grep -E '^0$'
- id: cp2
  criterion: 'harness/hooks/_lib.py: rg ''decompose-'' _lib.py → 0 строк'
  verify: rg 'plan/decompose-' harness/hooks/_lib.py | wc -l | grep -E '^0$'
- id: cp3
  criterion: 'mb_finish/finish_implement.py: rg hardcoded ''implement-'' → 0'
  verify: rg "'implement-" loop/mb_finish/finish_implement.py | wc -l | grep -E '^0$'
- id: cp4
  criterion: harness tests green после rewire
  verify: bin/pytest harness/hooks/tests/ -q --tb=short -x
verify: []
tdd: []
