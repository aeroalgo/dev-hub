schema: epic-decompose/v1
role: back
step_id: s06
plan_id: T-HUB-029-epic-phase-transition-engine
decompose_ref: memory-bank/back/plan/T-HUB-029-epic-phase-transition-engine/yaml/steps/s06-phase-registry-yaml-loader.yaml
title: "phase_registry.yaml + load_phase_registry() + gates_from_phase registry-driven"
next_phase: BACK IMPLEMENT
needs_creative: "no"

goal: |
  Создать `loop/schemas/phase_registry.yaml` — канонический phase registry (FR-011).
  Реализовать `load_phase_registry() -> dict` в `loop/epic_transition.py` — читает yaml, кэширует.
  Перепривязать `gates_from_phase(phase)` (epic/core.py) — вместо hardcoded dict → из registry.
  Unknown phase → fail-closed диагностика (не silent empty dict).
  Loader validation test: invalid yaml → ValueError с понятным сообщением.

context:
  consumes:
    - .claude/hooks/epic/core.py:539             # gates_from_phase — текущая hardcoded реализация
    - loop/epic_transition.py                    # s01-s05: resolve_next, arm_phase, promote_if_ready
    - loop/tests/test_stop_gate.py:1226          # test_gates_from_phase_ignores_terminal_phases — regression
    - loop/tests/test_creative_phase.py:11       # gates_from_phase import — regression
    - loop/schemas/                              # целевая директория для yaml
  produces:
    - loop/schemas/phase_registry.yaml           # NEW: канонический registry (все фазы)
    - loop/epic_transition.py                    # load_phase_registry(); gates_from_phase redirected
    - .claude/hooks/epic/core.py                 # gates_from_phase → load registry + lookup
    - loop/tests/test_epic_transition.py         # load_phase_registry tests + unknown phase fail-closed
    - loop/tests/test_stop_gate.py               # regression: test_gates_from_phase_* green
  plan_refs:
    - FR-010
    - FR-011
    - US-003
    - NFR-1
  files:
    - loop/schemas/phase_registry.yaml
    - loop/epic_transition.py
    - .claude/hooks/epic/core.py
    - loop/tests/test_epic_transition.py
    - loop/tests/test_stop_gate.py

as_built:
  - .claude/hooks/epic/core.py:539 — `gates_from_phase` — hardcoded dict mapping phase→gates; terminal phases (REFLECT, DONE) → пустой dict
  - loop/schemas/ — директория существует (runtime_adapters); нет phase_registry.yaml
  - нет load_phase_registry()
  - нет fail-closed для unknown phase

delta:
  - 'loop/schemas/phase_registry.yaml: NEW. Содержит все фазы (DECOMPOSE, ANALYZE, IMPLEMENT, TASK, REFACTOR, BUGFIX, QA, CLARIFY, PLAN, REFLECT, DONE). Каждая фаза: arm_template, finish_gates (cli/artifact), verify_agent, verify_optional_env?, promotable_after_finish, skip_index_sync, board_column. Полная таблица per plan §Architecture / phase_registry.yaml'
  - 'loop/epic_transition.py: `load_phase_registry(registry_path=None) -> dict` — читает yaml; кэширует в модуль-переменной; schema validate: must have "phases" key; ValidationError → ValueError с path'
  - 'loop/epic_transition.py: `get_phase_config(phase: str) -> dict` — lookup из load_phase_registry(); unknown phase → raise ValueError(f"unknown phase {phase!r}: fail-closed")'
  - '.claude/hooks/epic/core.py:gates_from_phase — заменить hardcoded dict на: `from loop.epic_transition import load_phase_registry; reg = load_phase_registry(); row = reg["phases"].get(phase); return row.get("finish_gates_dict", {})` + terminal phases → empty dict из registry.terminal_phases list'
  - 'loop/tests/test_epic_transition.py: test_load_phase_registry_returns_all_phases — len(registry["phases"]) >= 10'
  - 'loop/tests/test_epic_transition.py: test_get_phase_config_unknown_fails_closed — get_phase_config("BOGUS") → ValueError'
  - 'loop/tests/test_epic_transition.py: test_load_phase_registry_invalid_yaml_raises — corrupt yaml path → ValueError'
  - 'loop/tests/test_stop_gate.py: regression test_gates_from_phase_ignores_terminal_phases — REFLECT/DONE → {} (из registry terminal_phases)'

deletes:
  - '.claude/hooks/epic/core.py:gates_from_phase: hardcoded phase→gates dict body (заменяется на registry lookup)'

out_of_scope:
  - "verify agents wiring — s07"
  - "stop-gate registry-driven — s08"
  - "DSH presets — s09"

skills:
  code_surface: backend-core
  impl:
    - .agents/skills/tdd/SKILL.md
    - .agents/skills/python-testing-patterns/SKILL.md
    - .agents/skills/modern-python/SKILL.md

checkpoints:
  - id: cp1
    criterion: >
      loop/schemas/phase_registry.yaml создан; load_phase_registry() возвращает dict с ключом "phases"
    verify: "timeout 60s python -c 'from loop.epic_transition import load_phase_registry; r=load_phase_registry(); assert \"phases\" in r; print(list(r[\"phases\"].keys()))'"
  - id: cp2
    criterion: >
      test_get_phase_config_unknown_fails_closed PASS
    verify: "timeout 120s .venv/bin/pytest loop/tests/test_epic_transition.py::test_get_phase_config_unknown_fails_closed -q --tb=short"
  - id: cp3
    criterion: >
      Регрессия: test_gates_from_phase_ignores_terminal_phases PASS (REFLECT/DONE → {})
    verify: "timeout 120s .venv/bin/pytest loop/tests/test_stop_gate.py::test_gates_from_phase_ignores_terminal_phases loop/tests/test_creative_phase.py -q --tb=short"
  - id: cp4
    criterion: >
      Регрессия: test_stop_gate.py полный suite зелёный
    verify: "timeout 300s .venv/bin/pytest loop/tests/test_stop_gate.py -q --tb=line"

verify:
  - "timeout 300s .venv/bin/pytest loop/tests/test_epic_transition.py loop/tests/test_stop_gate.py loop/tests/test_creative_phase.py -q --tb=line"

tdd:
  - "test_load_phase_registry_returns_all_phases"
  - "test_get_phase_config_unknown_fails_closed"
  - "test_load_phase_registry_invalid_yaml_raises"
  - "Regression: test_gates_from_phase_ignores_terminal_phases"
  - "Regression: test_stop_gate.py full suite"
