schema: epic-implement/v1
role: back
step_id: s06
plan_id: T-HUB-046-harness-alongside-install
title: 's06 — bin/hub-unlink: --mode alongside — remove installer artifacts only IMPLEMENT'
status: completed
date: '2026-09-03'
decompose_ref: memory-bank/back/plan/decompose-T-HUB-046-harness-alongside-install/s06-hub-unlink-alongside.yaml
skills_used:
- writing-plans
discovery:
- bin/hub-unlink extended with --mode alongside|full
- strip_claude_md_block function added in loop/hub_claude_patch.py
- settings restore from .claude/settings.json.hub-backup integrated into alongside
  unlink
gaps:
  status: none
done:
- 'Added --mode alongside|full argument parsing to bin/hub-unlink (default: alongside)'
- 'Implemented alongside branch in bin/hub-unlink removing installer-owned artifacts:
  harness/, CLAUDE.harness.md, .dev-hub, .cursor/rules.d/dev-hub-harness-router.mdc'
- Implemented strip_claude_md_block in loop/hub_claude_patch.py and integrated into
  bin/hub-unlink
- Implemented .claude/settings.json.hub-backup restoration on unlink
- Added tests in loop/tests/test_hub_link_alongside.py and loop/tests/test_hub_settings_merge.py
files:
- bin/hub-unlink
- bin/hub-link
- loop/hub_claude_patch.py
- loop/tests/test_hub_link_alongside.py
- loop/tests/test_hub_settings_merge.py
deletes: []
tests:
- bin/pytest loop/tests/test_hub_link_alongside.py::test_alongside_unlink -q --tb=short
- bin/pytest loop/tests/test_hub_link_alongside.py::test_alongside_unlink_preserves_claude_body
  -q --tb=short
- bin/pytest loop/tests/test_hub_settings_merge.py::test_unlink_restores_settings
  -q --tb=short
- bin/pytest loop/tests/test_hub_link_alongside.py loop/tests/test_hub_settings_merge.py
  -q --tb=line
integration_check:
- bin/hub-unlink --help returns exit code 0 and documents --mode option
- bin/hub-unlink --mode alongside cleanly removes alongside install artifacts while
  preserving user files
grep_control:
- back: rg 'strip_claude_md_block' loop/
  front: ''
- back: rg 'alongside' bin/hub-unlink
  front: ''
verification_results:
- 'cp1: PASS'
- 'cp2: PASS'
- 'cp3: PASS'
checkpoints:
- id: cp1
  criterion: alongside unlink removes harness/ symlink and .dev-hub; CLAUDE.md user
    content intact
  status: done
- id: cp2
  criterion: unlink strips marker block; user lines outside markers preserved
  status: done
- id: cp3
  criterion: settings backup restored on unlink
  status: done
resume_from: cp1
