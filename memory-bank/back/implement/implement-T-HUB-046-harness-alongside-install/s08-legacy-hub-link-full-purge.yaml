schema: epic-implement/v1
role: back
step_id: s08
plan_id: T-HUB-046-harness-alongside-install
title: 's08 — Delete in-epic: purge implicit full-replace paths (hub-link no-mode
  = alongside now) IMPLEMENT'
status: completed
date: '2026-09-03'
decompose_ref: memory-bank/back/plan/decompose-T-HUB-046-harness-alongside-install/s08-legacy-hub-link-full-purge.yaml
skills_used:
- .agents/skills/tdd/SKILL.md
discovery:
- Audit via `rg 'hub-link' bin/ loop/ .github/` confirmed all active invocations specify
  `--mode` or test the alongside default; no scripts/callers implicitly expect full-replace.
gaps:
  status: none
done:
- Verified bin/hub-link default MODE is 'alongside' (MODE="alongside" default in arg
  parser).
- Audited all hub-link invocations in scripts and tests; legacy implicit full-replace
  behavior eliminated.
- Added test_alongside_is_default verifying clean install without --mode executes
  alongside install and does not create full-replace symlinks.
files:
- bin/hub-link
- loop/tests/test_hub_link_alongside.py
deletes:
- implicit default full-replace code path in bin/hub-link (replaced by alongside default
  in s03)
tests:
- '`bin/pytest loop/tests/test_hub_link_alongside.py::test_alongside_is_default -q
  --tb=line` — 1 passed'
- '`bin/pytest loop/tests/test_hub_link_alongside.py -q --tb=line` — 12 passed'
- '`bin/pytest loop/tests/test_hub_link_harness.py -q --tb=line` — 3 passed'
integration_check:
- '`rg ''hub-link'' bin/ .github/` shows only help documentation and tests; no unflagged
  full-replace assumptions'
- 'SC-004 validated: no silent fallback from alongside to full mode on conflict/error'
grep_control:
- back: ''
  front: ''
verification_results:
- 'cp1 PASS: audit confirms no implicit full-replace invocations in codebase/CI (`rg
  ''hub-link'' bin/ .github/`).'
- 'cp2 PASS: test_alongside_is_default confirms default execution without --mode is
  alongside mode (1 passed).'
- 'cp3 PASS: test_alongside_fails_on_conflict confirms SC-004 (fail-closed on conflict,
  no silent fallback).'
checkpoints:
- id: cp1
  criterion: 'grep hub-link callers: no calls without --mode flag that expect full-replace
    behavior'
  status: done
  done_at: '2026-09-03T00:00:01Z'
- id: cp2
  criterion: hub-link without --mode on clean fixture performs alongside (not full-replace)
  status: done
  done_at: '2026-09-03T00:00:02Z'
- id: cp3
  criterion: SC-004 — no silent fallback alongside→full in error path
  status: done
  done_at: '2026-09-03T00:00:03Z'
resume_from: cp1
