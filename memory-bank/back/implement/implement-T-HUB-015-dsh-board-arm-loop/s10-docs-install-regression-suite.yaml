schema: epic-implement/v1
role: back
step_id: s10
plan_id: T-HUB-015-dsh-board-arm-loop
title: s10 — Install script + docs runbook + regression suite — FR-009/010 IMPLEMENT
status: completed
date: '2026-08-29'
decompose_ref: memory-bank/back/plan/decompose-T-HUB-015-dsh-board-arm-loop/s10-docs-install-regression-suite.yaml
skills_used: []
discovery:
- Existing dsh/scripts/install-profiles.sh uses strict bash mode, repository-relative
  source paths, DSH_HOME defaults, and fail-closed diagnostics.
- Existing board sync tests exposed a CLI help compatibility issue; main now preserves
  argparse SystemExit for top-level --help while returning status codes for command
  errors.
gaps:
  status: none
done:
- Added executable dsh/scripts/install-mb-bridge.sh; validates DEV_HUB and source
  plugin, creates $DSH_HOME/plugins, and copies the private mb-bridge package.
- Added the Board Arm/Loop runbook with sync/filter/preset/arm+loop flow, model precedence,
  workspace filter behavior, install command, and flock/step_mismatch/DEV_HUB troubleshooting.
- Added regression smoke coverage importing all board_launch/board_sync modules and
  checking canonical CardKind/stable-id behavior.
- Preserved existing hub-board CLI argparse help behavior while keeping sync and launch
  errors as return codes.
files:
- dsh/scripts/install-mb-bridge.sh
- dsh/README.md
- loop/tests/test_board_launch_regression.py
- loop/board_sync/cli.py
deletes: []
tests:
- '`timeout 300s .venv/bin/pytest loop/tests/test_board_launch_regression.py -q --tb=line`'
- '`timeout 300s .venv/bin/pytest loop/tests/test_board_launch_*.py loop/tests/test_board_sync_*.py
  -q --tb=line`'
integration_check:
- dsh/scripts/install-mb-bridge.sh → dsh/plugins/mb-bridge → $DSH_HOME/plugins/mb-bridge
- dsh/README.md → board sync CLI, workspace filter, model precedence, and bridge troubleshooting
- loop/tests/test_board_launch_regression.py → loop.board_launch + loop.board_sync
  imports and card_model identity
grep_control:
- back: rg -n "Board Arm/Loop|model precedence|step_mismatch|flock|DEV_HUB" dsh/README.md
  front: n/a
- back: rg -n "plugins/mb-bridge|DEV_HUB|DSH_HOME" dsh/scripts/install-mb-bridge.sh
  front: n/a
verification_results:
- 'cp1: grep -q Board Arm/Loop/model precedence/step_mismatch — PASS.'
- 'cp2: targeted regression test — 15 passed.'
- 'cp2: combined board launch and sync suite — 124 passed.'
- 'cp3: DEV_HUB=/nonexistent installer exits 1 with missing-directory diagnostic —
  PASS.'
checkpoints:
- id: cp1
  criterion: 'dsh/README.md содержит разделы: ''## Board Arm/Loop'', model precedence
    table, troubleshooting'
  status: done
  done_at: '2026-08-29T00:00:00Z'
- id: cp2
  criterion: 'regression suite: timeout 300s .venv/bin/pytest loop/tests/test_board_launch_*.py
    loop/tests/test_board_sync_*.py -q — все зелёные'
  status: done
  done_at: '2026-08-29T00:00:00Z'
- id: cp3
  criterion: install-mb-bridge.sh существует; DEV_HUB=/nonexistent ./dsh/scripts/install-mb-bridge.sh
    → exit 1 с сообщением
  status: done
  done_at: '2026-08-29T00:00:00Z'
resume_from: cp1
