schema: epic-implement/v1
role: back
step_id: s05
plan_id: T-HUB-008-dsh-epic-gate-plugin
title: s05 — SessionStart first-turn semantics — DSH bridge gap close IMPLEMENT
status: completed
date: '2026-08-30'
decompose_ref: memory-bank/back/plan/decompose-T-HUB-008-dsh-epic-gate-plugin/s05-session-start-first-turn.yaml
skills_used:
- .agents/skills/tdd/SKILL.md
- .agents/skills/python-testing-patterns/SKILL.md
- .agents/skills/modern-python/SKILL.md
- .agents/skills/python-anti-patterns/SKILL.md
discovery:
- DSH `agent/session-start` payload has source=startup|resume|clear|compact and no
  is_first_turn/first_message field.
- The native event is emitted once before the first turn; `agent.inject()` is the
  supported startup injection point.
- The official Claude hook bridge maps SessionStart JSON additionalContext to `agent.inject()`
  and explicitly does not support initialUserMessage/sessionTitle.
gaps:
  status: none
done:
- 'Confirmed first-turn verdict: bridge-ok; no product code change is required.'
- Kept `.claude/hooks/session-start.py` as the source of truth for epic context; DSH
  native epic-gate already handles the first-turn lifecycle edge.
- Added DSH native SessionStart smoke coverage for startup and resume, including source
  attribution and unsupported initialUserMessage/sessionTitle absence.
files:
- .claude/hooks/session-start.py
- dsh/plugins/epic-gate/src/index.ts
- loop/tests/test_dsh_epic_gate_gaps.py
- dsh/README.md
deletes:
- n/a
tests:
- '`timeout 300s .venv/bin/pytest loop/tests/test_dsh_epic_gate_gaps.py -q --tb=line`
  — PASS (8 passed)'
- '`timeout 300s .venv/bin/pytest loop/tests/test_stop_gate.py -k session_start -q
  --tb=line` — PASS (1 passed, 55 deselected)'
- '`timeout 300s pnpm --dir dsh/plugins/epic-gate run typecheck` — PASS'
integration_check:
- '`rg -n "agent/session-start|SessionStartSource|initialUserMessage|additionalContext"
  dsh/plugins/epic-gate/node_modules/.pnpm/@deepseek-ai+dsh-agent@*/node_modules/@deepseek-ai/dsh-agent/lib/types/runtime-types.d.ts
  dsh/plugins/epic-gate/node_modules/.pnpm/@deepseek-ai+dsh-agent@*/node_modules/@deepseek-ai/dsh-agent/README.md`
  — DSH lifecycle and bridge contract verified.'
- '`rg -n "applySessionStart|agent/session-start|agent.inject|sessionStartInject"
  dsh/plugins/epic-gate/src/index.ts` — native handler registration and agents injection
  are connected.'
- '`rg -n "SessionStart|first-turn|bridge-ok|additionalContext" dsh/README.md loop/tests/test_dsh_epic_gate_gaps.py`
  — documented verdict and executable smoke coverage are connected.'
grep_control:
- back: '`rg -n "first-turn verdict: (bridge-ok|gap-confirmed)" memory-bank/back/plan/decompose-T-HUB-008-dsh-epic-gate-plugin/s05-session-start-first-turn.yaml`'
  front: ''
verification_results:
- 'cp1 PASS: DSH contract documents source=startup|resume and native first-turn lifecycle;
  verdict is bridge-ok.'
- 'cp2 PASS: DSH SessionStart smoke confirms context injection at startup and resume
  without a reset path.'
- The existing Python SessionStart regression remains green.
checkpoints:
- id: cp1
  criterion: DSH SessionStart first-turn задокументирован (bridge-ok|gap-confirmed)
  status: done
  done_at: '2026-08-30T15:20:00Z'
- id: cp2
  criterion: Если gap-confirmed — session-start/epic-gate обеспечивают корректный
    first-turn; тесты зелёные
  status: done
  done_at: '2026-08-30T15:20:01Z'
resume_from: cp1
