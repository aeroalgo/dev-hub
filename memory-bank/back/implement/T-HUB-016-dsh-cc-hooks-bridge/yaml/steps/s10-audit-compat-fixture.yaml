schema: epic-implement/v1
role: back
step_id: s10
plan_id: T-HUB-016
title: s10 — Audit remediation — compat skill fixture and fail-soft smoke IMPLEMENT
status: completed
date: '2026-08-29'
decompose_ref: memory-bank/back/plan/T-HUB-016-dsh-cc-hooks-bridge/yaml/steps/s10-audit-compat-fixture.yaml
skills_used: []
discovery:
- 'graphify N/A: hub repository has no root graphify-out/graph.json and no .venv/bin/graphify;
  bounded DSH paths inspected directly.'
gaps:
  status: none
done:
- Removed the duplicate epic-implement compat include and guarded the remaining optional
  include before Cordis import resolution; DSH_CC_COMPAT=0 still disables it.
- Isolated project fixture listed skill s10-compat-fixture through the compat provider
  with source project-claude and no user-level fixture.
- Fresh DSH_HOME profile with dsh-claude-compat removed emitted the optional-package
  warning and completed --help with exit 0; the required cc-hooks-bridge remained
  in the composed profile.
files:
- dsh/profiles/epic-implement/cordis.patch.yml
- memory-bank/back/implement/T-HUB-016-dsh-cc-hooks-bridge/yaml/steps/s10-audit-compat-fixture.yaml
deletes: []
tests:
- '`timeout 300s .venv/bin/pytest tests/test_cc_hooks_bridge_config.py -q --tb=line`'
- '`timeout 300s bash dsh/scripts/install-profiles.sh --dry-run`'
- '`timeout 300s dsh --profile epic-implement --help`'
integration_check:
- The profile-level guard resolves dsh-claude-compat from the profile package root
  before the nested include is imported, so an unavailable optional package cannot
  fail the include tree; the required cc-hooks-bridge remains a separate entry.
grep_control:
- back: rg -n -A16 -B4 "cc-hooks-bridge|cc-claude-compat|optional dsh-claude-compat|skipping"
    /tmp/dsh-s10-missing-help.log — matched warning and retained profile boot output.
  front: n/a
- back: Inline provider assertion candidate.name === s10-compat-fixture and candidate.source
    === project-claude — matched.
  front: n/a
verification_results:
- 'cp1 PASS: isolated .claude/skills/s10-compat-fixture was listed by the enabled
  claude-compat provider.'
- 'cp2 PASS: missing optional package emitted the skip warning and profile --help
  returned exit 0; DSH_CC_COMPAT=0 also retained the required bridge entry.'
checkpoints:
- id: cp1
  criterion: A .claude/skills fixture is listed by the enabled compat mount.
  status: done
  done_at: '2026-08-29T18:00:00Z'
- id: cp2
  criterion: Missing optional compat package emits a warning and does not block profile
    boot.
  status: done
  done_at: '2026-08-29T18:00:00Z'
resume_from: cp1
