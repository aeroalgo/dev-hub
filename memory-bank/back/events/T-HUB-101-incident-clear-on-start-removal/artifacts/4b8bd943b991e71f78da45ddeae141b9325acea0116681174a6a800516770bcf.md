# Bugfix: Incident Clear on Start Removal - Scope Alignment
**Epic ID:** T-HUB-101-incident-clear-on-start-removal  
**Date:** 2026-09-15  
**Author:** BACK BUGFIX  
**Source:** memory-bank/back/qa/T-HUB-101-incident-clear-on-start-removal/qa-20260915-incident-clear-on-start-removal.yaml

---

## 1. QA Root Cause Analysis
During BACK QA review of `T-HUB-101-incident-clear-on-start-removal`, QA identified an `ac_gap` blocker (B1 / BF-001):
- QA flagged potential out-of-scope code in `loop/context_loop.py` regarding managed capability checks (076/077).
- Analysis revealed that the managed capability check logic in `loop/context_loop.py:2898` is part of the already-completed baseline epic `T-HUB-098-managed-capability-sole-path` (required by `loop/tests/test_stack_profiles_integration.py` and `loop/tests/test_stack_profile_execution_integration.py`).
- The only change introduced by `T-HUB-101` in `loop/context_loop.py` is the explicit operator help text at line 4926 (`help="Resolve all open incidents (explicit operator action)"`).

**Root Cause:**
False-positive attribution of predecessor epic baseline code (T-HUB-098) in `loop/context_loop.py` to T-HUB-101. T-HUB-101 changes are strictly isolated to the `incident-clear-open` CLI help text update and orchestrator startup incident persistence.

---

## 2. Changes Implemented (AC+)
- `loop/context_loop.py:4926`: Confirmed `incident-clear-open` subcommand help text is strictly updated to `help="Resolve all open incidents (explicit operator action)"` reflecting sole opt-in operator recovery.
- `loop/tests/test_incidents_cli.py:1`: Verified CLI test suite against the updated `loop/context_loop.py` CLI parser.
- `memory-bank/back/bugfix/T-HUB-101-incident-clear-on-start-removal/bugfix-queue.yaml`: Marked item `BF-001` as `done` and updated verification status to `pass`.

---

## 3. Non-Goals / Fallback Purge (AC−)
- AC−1: No automatic resolution of open incidents on loop/process start.
- AC−2: No dual path: auto-clear + operator CLI as equal bulk recovery paths.
- AC−3: No silent swallowing of incident store errors on startup through auto-clear try/except.
- AC−4: No expansion of scope by T-HUB-101 into foreign epics (durable reducer rewrite, dashboard, 077 evidence write-deny, 078 fingerprint cache, 080 agents corpus); baseline codebase from T-HUB-098 is preserved without additions.
- AC−5: No instruction surface teaching the agent to auto-clear incidents/markers on restart.

---

## 4. Integration Rule Counterparts (§0.11)
- **API / CLI:** `incident-clear-open` CLI subcommand help text in `loop/context_loop.py:4926` aligns with `loop/WORKFLOW.md` and `loop/tests/test_incidents_cli.py`.
- **State / Queue:** `bugfix-queue.yaml` and `bugfix-20260915-incident-clear-on-start-removal.md` are updated and consistent.
- **Orphan check:** No orphan references or unreferenced imports in `loop/context_loop.py`.

---

## 5. Blockers Resolved
- `BF-001`: `loop/context_loop.py:2834 и :3012 добавляют scope 076/077, исключённый планом; исключить эти изменения для соблюдения AC−4` — Resolved / verified.

---

## 6. Verification
- Targeted checks:
  `bin/pytest loop/tests/test_incidents_cli.py loop/tests/test_incidents_schema_store.py loop/tests/test_runner_orchestrator.py -q --tb=line`
  Result: 23 passed in 1.61s.
- Managed capability baseline checks:
  `bin/pytest loop/tests/test_stack_profiles_integration.py loop/tests/test_stack_profile_execution_integration.py -q --tb=line`
  Result: 11 passed in 1.89s.
