[pytest]
pythonpath = . .claude/hooks loop harness/hooks
addopts = -n 4 --dist loadfile
timeout = 120
timeout_method = signal
markers =
    ac: living acceptance criterion marker (links plan req to test)
