---
description: back DECOMPOSE — разложить готовый план на атомарные шаги (Cursor parity)
---
Apply `.claude/skills/role-command/SKILL.md` for **back DECOMPOSE**.

**HARD:** maximal detail — все этапы + все AC/FR/NFR плана в `decompose-*/` (`## Requirements coverage`, `## Stages coverage`, `## Outcome map`, `## Replacement cleanup`). Brownfield replace → `deletes:` + `rg` cp. Не ужимать число шагов. См. `.cursor/rules/back_developer/workflow-decompose.mdc` §Maximal detail + §Replacement cleanup + `.claude/rules/plan-artifact.md`.

$ARGUMENTS
