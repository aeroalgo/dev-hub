# Пример фрагментов для DSH MCP (НЕ копируй слепо — встрой в свой profile patch).
# Docs: https://deepseekdocs.com/en/docs/features/mcp
#
# Имена пакетов/полей сверяй со своей версией dsh (`dsh web --dump-config`).

# --- 1) Kinocut / video assemble (когда поставишь) ---
# - id: mcp-kinocut
#   name: '@deepseek-ai/dsh-mcp-client'
#   config:
#     serverName: kinocut
#     transport: stdio
#     command: uvx
#     args: ['--from', 'kinocut', 'kino']
#     toolCallTimeoutMs: 600000
#     cwd: /home/aero/VideoProject/test_project

# --- 2) Позже: image / video gen API как отдельный MCP ---
# - id: mcp-runway
#   name: '@deepseek-ai/dsh-mcp-client'
#   config:
#     serverName: runway
#     transport: stdio
#     command: node
#     args: ['/path/to/your-runway-mcp/server.js']
#     toolCallTimeoutMs: 900000
#     env:
#       RUNWAY_API_KEY: !!js process.env.RUNWAY_API_KEY

# Пока MCP нет — агент только правит yaml (фаза plan).
